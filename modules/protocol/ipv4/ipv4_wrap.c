/* ----------------------------------------------------------------------------
 * This file was automatically generated by SWIG (http://www.swig.org).
 * Version 2.0.7
 * 
 * This file is not intended to be easily readable and contains a number of 
 * coding conventions designed to improve portability and efficiency. Do not make
 * changes to this file unless you know what you are doing--modify the SWIG 
 * interface file instead. 
 * ----------------------------------------------------------------------------- */

#define SWIGLUA
#define SWIG_LUA_TARGET SWIG_LUA_FLAVOR_LUA
#define SWIG_LUA_NO_MODULE_GLOBAL

/* -----------------------------------------------------------------------------
 *  This section contains generic SWIG labels for method/variable
 *  declarations/attributes, and other compiler dependent labels.
 * ----------------------------------------------------------------------------- */

/* template workaround for compilers that cannot correctly implement the C++ standard */
#ifndef SWIGTEMPLATEDISAMBIGUATOR
# if defined(__SUNPRO_CC) && (__SUNPRO_CC <= 0x560)
#  define SWIGTEMPLATEDISAMBIGUATOR template
# elif defined(__HP_aCC)
/* Needed even with `aCC -AA' when `aCC -V' reports HP ANSI C++ B3910B A.03.55 */
/* If we find a maximum version that requires this, the test would be __HP_aCC <= 35500 for A.03.55 */
#  define SWIGTEMPLATEDISAMBIGUATOR template
# else
#  define SWIGTEMPLATEDISAMBIGUATOR
# endif
#endif

/* inline attribute */
#ifndef SWIGINLINE
# if defined(__cplusplus) || (defined(__GNUC__) && !defined(__STRICT_ANSI__))
#   define SWIGINLINE inline
# else
#   define SWIGINLINE
# endif
#endif

/* attribute recognised by some compilers to avoid 'unused' warnings */
#ifndef SWIGUNUSED
# if defined(__GNUC__)
#   if !(defined(__cplusplus)) || (__GNUC__ > 3 || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4))
#     define SWIGUNUSED __attribute__ ((__unused__)) 
#   else
#     define SWIGUNUSED
#   endif
# elif defined(__ICC)
#   define SWIGUNUSED __attribute__ ((__unused__)) 
# else
#   define SWIGUNUSED 
# endif
#endif

#ifndef SWIG_MSC_UNSUPPRESS_4505
# if defined(_MSC_VER)
#   pragma warning(disable : 4505) /* unreferenced local function has been removed */
# endif 
#endif

#ifndef SWIGUNUSEDPARM
# ifdef __cplusplus
#   define SWIGUNUSEDPARM(p)
# else
#   define SWIGUNUSEDPARM(p) p SWIGUNUSED 
# endif
#endif

/* internal SWIG method */
#ifndef SWIGINTERN
# define SWIGINTERN static SWIGUNUSED
#endif

/* internal inline SWIG method */
#ifndef SWIGINTERNINLINE
# define SWIGINTERNINLINE SWIGINTERN SWIGINLINE
#endif

/* exporting methods */
#if (__GNUC__ >= 4) || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4)
#  ifndef GCC_HASCLASSVISIBILITY
#    define GCC_HASCLASSVISIBILITY
#  endif
#endif

#ifndef SWIGEXPORT
# if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)
#   if defined(STATIC_LINKED)
#     define SWIGEXPORT
#   else
#     define SWIGEXPORT __declspec(dllexport)
#   endif
# else
#   if defined(__GNUC__) && defined(GCC_HASCLASSVISIBILITY)
#     define SWIGEXPORT __attribute__ ((visibility("default")))
#   else
#     define SWIGEXPORT
#   endif
# endif
#endif

/* calling conventions for Windows */
#ifndef SWIGSTDCALL
# if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)
#   define SWIGSTDCALL __stdcall
# else
#   define SWIGSTDCALL
# endif 
#endif

/* Deal with Microsoft's attempt at deprecating C standard runtime functions */
#if !defined(SWIG_NO_CRT_SECURE_NO_DEPRECATE) && defined(_MSC_VER) && !defined(_CRT_SECURE_NO_DEPRECATE)
# define _CRT_SECURE_NO_DEPRECATE
#endif

/* Deal with Microsoft's attempt at deprecating methods in the standard C++ library */
#if !defined(SWIG_NO_SCL_SECURE_NO_DEPRECATE) && defined(_MSC_VER) && !defined(_SCL_SECURE_NO_DEPRECATE)
# define _SCL_SECURE_NO_DEPRECATE
#endif


/* -----------------------------------------------------------------------------
 * swigrun.swg
 *
 * This file contains generic C API SWIG runtime support for pointer
 * type checking.
 * ----------------------------------------------------------------------------- */

/* This should only be incremented when either the layout of swig_type_info changes,
   or for whatever reason, the runtime changes incompatibly */
#define SWIG_RUNTIME_VERSION "4"

/* define SWIG_TYPE_TABLE_NAME as "SWIG_TYPE_TABLE" */
#ifdef SWIG_TYPE_TABLE
# define SWIG_QUOTE_STRING(x) #x
# define SWIG_EXPAND_AND_QUOTE_STRING(x) SWIG_QUOTE_STRING(x)
# define SWIG_TYPE_TABLE_NAME SWIG_EXPAND_AND_QUOTE_STRING(SWIG_TYPE_TABLE)
#else
# define SWIG_TYPE_TABLE_NAME
#endif

/*
  You can use the SWIGRUNTIME and SWIGRUNTIMEINLINE macros for
  creating a static or dynamic library from the SWIG runtime code.
  In 99.9% of the cases, SWIG just needs to declare them as 'static'.
  
  But only do this if strictly necessary, ie, if you have problems
  with your compiler or suchlike.
*/

#ifndef SWIGRUNTIME
# define SWIGRUNTIME SWIGINTERN
#endif

#ifndef SWIGRUNTIMEINLINE
# define SWIGRUNTIMEINLINE SWIGRUNTIME SWIGINLINE
#endif

/*  Generic buffer size */
#ifndef SWIG_BUFFER_SIZE
# define SWIG_BUFFER_SIZE 1024
#endif

/* Flags for pointer conversions */
#define SWIG_POINTER_DISOWN        0x1
#define SWIG_CAST_NEW_MEMORY       0x2

/* Flags for new pointer objects */
#define SWIG_POINTER_OWN           0x1


/* 
   Flags/methods for returning states.
   
   The SWIG conversion methods, as ConvertPtr, return an integer 
   that tells if the conversion was successful or not. And if not,
   an error code can be returned (see swigerrors.swg for the codes).
   
   Use the following macros/flags to set or process the returning
   states.
   
   In old versions of SWIG, code such as the following was usually written:

     if (SWIG_ConvertPtr(obj,vptr,ty.flags) != -1) {
       // success code
     } else {
       //fail code
     }

   Now you can be more explicit:

    int res = SWIG_ConvertPtr(obj,vptr,ty.flags);
    if (SWIG_IsOK(res)) {
      // success code
    } else {
      // fail code
    }

   which is the same really, but now you can also do

    Type *ptr;
    int res = SWIG_ConvertPtr(obj,(void **)(&ptr),ty.flags);
    if (SWIG_IsOK(res)) {
      // success code
      if (SWIG_IsNewObj(res) {
        ...
	delete *ptr;
      } else {
        ...
      }
    } else {
      // fail code
    }
    
   I.e., now SWIG_ConvertPtr can return new objects and you can
   identify the case and take care of the deallocation. Of course that
   also requires SWIG_ConvertPtr to return new result values, such as

      int SWIG_ConvertPtr(obj, ptr,...) {         
        if (<obj is ok>) {			       
          if (<need new object>) {		       
            *ptr = <ptr to new allocated object>; 
            return SWIG_NEWOBJ;		       
          } else {				       
            *ptr = <ptr to old object>;	       
            return SWIG_OLDOBJ;		       
          } 				       
        } else {				       
          return SWIG_BADOBJ;		       
        }					       
      }

   Of course, returning the plain '0(success)/-1(fail)' still works, but you can be
   more explicit by returning SWIG_BADOBJ, SWIG_ERROR or any of the
   SWIG errors code.

   Finally, if the SWIG_CASTRANK_MODE is enabled, the result code
   allows to return the 'cast rank', for example, if you have this

       int food(double)
       int fooi(int);

   and you call
 
      food(1)   // cast rank '1'  (1 -> 1.0)
      fooi(1)   // cast rank '0'

   just use the SWIG_AddCast()/SWIG_CheckState()
*/

#define SWIG_OK                    (0) 
#define SWIG_ERROR                 (-1)
#define SWIG_IsOK(r)               (r >= 0)
#define SWIG_ArgError(r)           ((r != SWIG_ERROR) ? r : SWIG_TypeError)  

/* The CastRankLimit says how many bits are used for the cast rank */
#define SWIG_CASTRANKLIMIT         (1 << 8)
/* The NewMask denotes the object was created (using new/malloc) */
#define SWIG_NEWOBJMASK            (SWIG_CASTRANKLIMIT  << 1)
/* The TmpMask is for in/out typemaps that use temporal objects */
#define SWIG_TMPOBJMASK            (SWIG_NEWOBJMASK << 1)
/* Simple returning values */
#define SWIG_BADOBJ                (SWIG_ERROR)
#define SWIG_OLDOBJ                (SWIG_OK)
#define SWIG_NEWOBJ                (SWIG_OK | SWIG_NEWOBJMASK)
#define SWIG_TMPOBJ                (SWIG_OK | SWIG_TMPOBJMASK)
/* Check, add and del mask methods */
#define SWIG_AddNewMask(r)         (SWIG_IsOK(r) ? (r | SWIG_NEWOBJMASK) : r)
#define SWIG_DelNewMask(r)         (SWIG_IsOK(r) ? (r & ~SWIG_NEWOBJMASK) : r)
#define SWIG_IsNewObj(r)           (SWIG_IsOK(r) && (r & SWIG_NEWOBJMASK))
#define SWIG_AddTmpMask(r)         (SWIG_IsOK(r) ? (r | SWIG_TMPOBJMASK) : r)
#define SWIG_DelTmpMask(r)         (SWIG_IsOK(r) ? (r & ~SWIG_TMPOBJMASK) : r)
#define SWIG_IsTmpObj(r)           (SWIG_IsOK(r) && (r & SWIG_TMPOBJMASK))

/* Cast-Rank Mode */
#if defined(SWIG_CASTRANK_MODE)
#  ifndef SWIG_TypeRank
#    define SWIG_TypeRank             unsigned long
#  endif
#  ifndef SWIG_MAXCASTRANK            /* Default cast allowed */
#    define SWIG_MAXCASTRANK          (2)
#  endif
#  define SWIG_CASTRANKMASK          ((SWIG_CASTRANKLIMIT) -1)
#  define SWIG_CastRank(r)           (r & SWIG_CASTRANKMASK)
SWIGINTERNINLINE int SWIG_AddCast(int r) { 
  return SWIG_IsOK(r) ? ((SWIG_CastRank(r) < SWIG_MAXCASTRANK) ? (r + 1) : SWIG_ERROR) : r;
}
SWIGINTERNINLINE int SWIG_CheckState(int r) { 
  return SWIG_IsOK(r) ? SWIG_CastRank(r) + 1 : 0; 
}
#else /* no cast-rank mode */
#  define SWIG_AddCast
#  define SWIG_CheckState(r) (SWIG_IsOK(r) ? 1 : 0)
#endif


#include <string.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef void *(*swig_converter_func)(void *, int *);
typedef struct swig_type_info *(*swig_dycast_func)(void **);

/* Structure to store information on one type */
typedef struct swig_type_info {
  const char             *name;			/* mangled name of this type */
  const char             *str;			/* human readable name of this type */
  swig_dycast_func        dcast;		/* dynamic cast function down a hierarchy */
  struct swig_cast_info  *cast;			/* linked list of types that can cast into this type */
  void                   *clientdata;		/* language specific type data */
  int                    owndata;		/* flag if the structure owns the clientdata */
} swig_type_info;

/* Structure to store a type and conversion function used for casting */
typedef struct swig_cast_info {
  swig_type_info         *type;			/* pointer to type that is equivalent to this type */
  swig_converter_func     converter;		/* function to cast the void pointers */
  struct swig_cast_info  *next;			/* pointer to next cast in linked list */
  struct swig_cast_info  *prev;			/* pointer to the previous cast */
} swig_cast_info;

/* Structure used to store module information
 * Each module generates one structure like this, and the runtime collects
 * all of these structures and stores them in a circularly linked list.*/
typedef struct swig_module_info {
  swig_type_info         **types;		/* Array of pointers to swig_type_info structures that are in this module */
  size_t                 size;		        /* Number of types in this module */
  struct swig_module_info *next;		/* Pointer to next element in circularly linked list */
  swig_type_info         **type_initial;	/* Array of initially generated type structures */
  swig_cast_info         **cast_initial;	/* Array of initially generated casting structures */
  void                    *clientdata;		/* Language specific module data */
} swig_module_info;

/* 
  Compare two type names skipping the space characters, therefore
  "char*" == "char *" and "Class<int>" == "Class<int >", etc.

  Return 0 when the two name types are equivalent, as in
  strncmp, but skipping ' '.
*/
SWIGRUNTIME int
SWIG_TypeNameComp(const char *f1, const char *l1,
		  const char *f2, const char *l2) {
  for (;(f1 != l1) && (f2 != l2); ++f1, ++f2) {
    while ((*f1 == ' ') && (f1 != l1)) ++f1;
    while ((*f2 == ' ') && (f2 != l2)) ++f2;
    if (*f1 != *f2) return (*f1 > *f2) ? 1 : -1;
  }
  return (int)((l1 - f1) - (l2 - f2));
}

/*
  Check type equivalence in a name list like <name1>|<name2>|...
  Return 0 if not equal, 1 if equal
*/
SWIGRUNTIME int
SWIG_TypeEquiv(const char *nb, const char *tb) {
  int equiv = 0;
  const char* te = tb + strlen(tb);
  const char* ne = nb;
  while (!equiv && *ne) {
    for (nb = ne; *ne; ++ne) {
      if (*ne == '|') break;
    }
    equiv = (SWIG_TypeNameComp(nb, ne, tb, te) == 0) ? 1 : 0;
    if (*ne) ++ne;
  }
  return equiv;
}

/*
  Check type equivalence in a name list like <name1>|<name2>|...
  Return 0 if equal, -1 if nb < tb, 1 if nb > tb
*/
SWIGRUNTIME int
SWIG_TypeCompare(const char *nb, const char *tb) {
  int equiv = 0;
  const char* te = tb + strlen(tb);
  const char* ne = nb;
  while (!equiv && *ne) {
    for (nb = ne; *ne; ++ne) {
      if (*ne == '|') break;
    }
    equiv = (SWIG_TypeNameComp(nb, ne, tb, te) == 0) ? 1 : 0;
    if (*ne) ++ne;
  }
  return equiv;
}


/*
  Check the typename
*/
SWIGRUNTIME swig_cast_info *
SWIG_TypeCheck(const char *c, swig_type_info *ty) {
  if (ty) {
    swig_cast_info *iter = ty->cast;
    while (iter) {
      if (strcmp(iter->type->name, c) == 0) {
        if (iter == ty->cast)
          return iter;
        /* Move iter to the top of the linked list */
        iter->prev->next = iter->next;
        if (iter->next)
          iter->next->prev = iter->prev;
        iter->next = ty->cast;
        iter->prev = 0;
        if (ty->cast) ty->cast->prev = iter;
        ty->cast = iter;
        return iter;
      }
      iter = iter->next;
    }
  }
  return 0;
}

/* 
  Identical to SWIG_TypeCheck, except strcmp is replaced with a pointer comparison
*/
SWIGRUNTIME swig_cast_info *
SWIG_TypeCheckStruct(swig_type_info *from, swig_type_info *ty) {
  if (ty) {
    swig_cast_info *iter = ty->cast;
    while (iter) {
      if (iter->type == from) {
        if (iter == ty->cast)
          return iter;
        /* Move iter to the top of the linked list */
        iter->prev->next = iter->next;
        if (iter->next)
          iter->next->prev = iter->prev;
        iter->next = ty->cast;
        iter->prev = 0;
        if (ty->cast) ty->cast->prev = iter;
        ty->cast = iter;
        return iter;
      }
      iter = iter->next;
    }
  }
  return 0;
}

/*
  Cast a pointer up an inheritance hierarchy
*/
SWIGRUNTIMEINLINE void *
SWIG_TypeCast(swig_cast_info *ty, void *ptr, int *newmemory) {
  return ((!ty) || (!ty->converter)) ? ptr : (*ty->converter)(ptr, newmemory);
}

/* 
   Dynamic pointer casting. Down an inheritance hierarchy
*/
SWIGRUNTIME swig_type_info *
SWIG_TypeDynamicCast(swig_type_info *ty, void **ptr) {
  swig_type_info *lastty = ty;
  if (!ty || !ty->dcast) return ty;
  while (ty && (ty->dcast)) {
    ty = (*ty->dcast)(ptr);
    if (ty) lastty = ty;
  }
  return lastty;
}

/*
  Return the name associated with this type
*/
SWIGRUNTIMEINLINE const char *
SWIG_TypeName(const swig_type_info *ty) {
  return ty->name;
}

/*
  Return the pretty name associated with this type,
  that is an unmangled type name in a form presentable to the user.
*/
SWIGRUNTIME const char *
SWIG_TypePrettyName(const swig_type_info *type) {
  /* The "str" field contains the equivalent pretty names of the
     type, separated by vertical-bar characters.  We choose
     to print the last name, as it is often (?) the most
     specific. */
  if (!type) return NULL;
  if (type->str != NULL) {
    const char *last_name = type->str;
    const char *s;
    for (s = type->str; *s; s++)
      if (*s == '|') last_name = s+1;
    return last_name;
  }
  else
    return type->name;
}

/* 
   Set the clientdata field for a type
*/
SWIGRUNTIME void
SWIG_TypeClientData(swig_type_info *ti, void *clientdata) {
  swig_cast_info *cast = ti->cast;
  /* if (ti->clientdata == clientdata) return; */
  ti->clientdata = clientdata;
  
  while (cast) {
    if (!cast->converter) {
      swig_type_info *tc = cast->type;
      if (!tc->clientdata) {
	SWIG_TypeClientData(tc, clientdata);
      }
    }    
    cast = cast->next;
  }
}
SWIGRUNTIME void
SWIG_TypeNewClientData(swig_type_info *ti, void *clientdata) {
  SWIG_TypeClientData(ti, clientdata);
  ti->owndata = 1;
}
  
/*
  Search for a swig_type_info structure only by mangled name
  Search is a O(log #types)
  
  We start searching at module start, and finish searching when start == end.  
  Note: if start == end at the beginning of the function, we go all the way around
  the circular list.
*/
SWIGRUNTIME swig_type_info *
SWIG_MangledTypeQueryModule(swig_module_info *start, 
                            swig_module_info *end, 
		            const char *name) {
  swig_module_info *iter = start;
  do {
    if (iter->size) {
      register size_t l = 0;
      register size_t r = iter->size - 1;
      do {
	/* since l+r >= 0, we can (>> 1) instead (/ 2) */
	register size_t i = (l + r) >> 1; 
	const char *iname = iter->types[i]->name;
	if (iname) {
	  register int compare = strcmp(name, iname);
	  if (compare == 0) {	    
	    return iter->types[i];
	  } else if (compare < 0) {
	    if (i) {
	      r = i - 1;
	    } else {
	      break;
	    }
	  } else if (compare > 0) {
	    l = i + 1;
	  }
	} else {
	  break; /* should never happen */
	}
      } while (l <= r);
    }
    iter = iter->next;
  } while (iter != end);
  return 0;
}

/*
  Search for a swig_type_info structure for either a mangled name or a human readable name.
  It first searches the mangled names of the types, which is a O(log #types)
  If a type is not found it then searches the human readable names, which is O(#types).
  
  We start searching at module start, and finish searching when start == end.  
  Note: if start == end at the beginning of the function, we go all the way around
  the circular list.
*/
SWIGRUNTIME swig_type_info *
SWIG_TypeQueryModule(swig_module_info *start, 
                     swig_module_info *end, 
		     const char *name) {
  /* STEP 1: Search the name field using binary search */
  swig_type_info *ret = SWIG_MangledTypeQueryModule(start, end, name);
  if (ret) {
    return ret;
  } else {
    /* STEP 2: If the type hasn't been found, do a complete search
       of the str field (the human readable name) */
    swig_module_info *iter = start;
    do {
      register size_t i = 0;
      for (; i < iter->size; ++i) {
	if (iter->types[i]->str && (SWIG_TypeEquiv(iter->types[i]->str, name)))
	  return iter->types[i];
      }
      iter = iter->next;
    } while (iter != end);
  }
  
  /* neither found a match */
  return 0;
}

/* 
   Pack binary data into a string
*/
SWIGRUNTIME char *
SWIG_PackData(char *c, void *ptr, size_t sz) {
  static const char hex[17] = "0123456789abcdef";
  register const unsigned char *u = (unsigned char *) ptr;
  register const unsigned char *eu =  u + sz;
  for (; u != eu; ++u) {
    register unsigned char uu = *u;
    *(c++) = hex[(uu & 0xf0) >> 4];
    *(c++) = hex[uu & 0xf];
  }
  return c;
}

/* 
   Unpack binary data from a string
*/
SWIGRUNTIME const char *
SWIG_UnpackData(const char *c, void *ptr, size_t sz) {
  register unsigned char *u = (unsigned char *) ptr;
  register const unsigned char *eu = u + sz;
  for (; u != eu; ++u) {
    register char d = *(c++);
    register unsigned char uu;
    if ((d >= '0') && (d <= '9'))
      uu = ((d - '0') << 4);
    else if ((d >= 'a') && (d <= 'f'))
      uu = ((d - ('a'-10)) << 4);
    else 
      return (char *) 0;
    d = *(c++);
    if ((d >= '0') && (d <= '9'))
      uu |= (d - '0');
    else if ((d >= 'a') && (d <= 'f'))
      uu |= (d - ('a'-10));
    else 
      return (char *) 0;
    *u = uu;
  }
  return c;
}

/* 
   Pack 'void *' into a string buffer.
*/
SWIGRUNTIME char *
SWIG_PackVoidPtr(char *buff, void *ptr, const char *name, size_t bsz) {
  char *r = buff;
  if ((2*sizeof(void *) + 2) > bsz) return 0;
  *(r++) = '_';
  r = SWIG_PackData(r,&ptr,sizeof(void *));
  if (strlen(name) + 1 > (bsz - (r - buff))) return 0;
  strcpy(r,name);
  return buff;
}

SWIGRUNTIME const char *
SWIG_UnpackVoidPtr(const char *c, void **ptr, const char *name) {
  if (*c != '_') {
    if (strcmp(c,"NULL") == 0) {
      *ptr = (void *) 0;
      return name;
    } else {
      return 0;
    }
  }
  return SWIG_UnpackData(++c,ptr,sizeof(void *));
}

SWIGRUNTIME char *
SWIG_PackDataName(char *buff, void *ptr, size_t sz, const char *name, size_t bsz) {
  char *r = buff;
  size_t lname = (name ? strlen(name) : 0);
  if ((2*sz + 2 + lname) > bsz) return 0;
  *(r++) = '_';
  r = SWIG_PackData(r,ptr,sz);
  if (lname) {
    strncpy(r,name,lname+1);
  } else {
    *r = 0;
  }
  return buff;
}

SWIGRUNTIME const char *
SWIG_UnpackDataName(const char *c, void *ptr, size_t sz, const char *name) {
  if (*c != '_') {
    if (strcmp(c,"NULL") == 0) {
      memset(ptr,0,sz);
      return name;
    } else {
      return 0;
    }
  }
  return SWIG_UnpackData(++c,ptr,sz);
}

#ifdef __cplusplus
}
#endif

/* -----------------------------------------------------------------------------
 * luarun.swg
 *
 * This file contains the runtime support for Lua modules
 * and includes code for managing global variables and pointer
 * type checking.
 * ----------------------------------------------------------------------------- */

#ifdef __cplusplus
extern "C" {
#endif

#include "lua.h"
#include "lauxlib.h"
#include <stdlib.h>  /* for malloc */
#include <assert.h>  /* for a few sanity tests */

/* -----------------------------------------------------------------------------
 * Lua flavors
 * ----------------------------------------------------------------------------- */

#define SWIG_LUA_FLAVOR_LUA 1
#define SWIG_LUA_FLAVOR_ELUA 2
#define SWIG_LUA_FLAVOR_ELUAC 3

#if !defined(SWIG_LUA_TARGET)
# error SWIG_LUA_TARGET not defined
#endif

#if (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC)
#  define SWIG_LUA_CONSTTAB_INT(B, C) LSTRKEY(B), LNUMVAL(C)
#  define SWIG_LUA_CONSTTAB_FLOAT(B, C) LSTRKEY(B), LNUMVAL(C)
#  define SWIG_LUA_CONSTTAB_STRING(B, C) LSTRKEY(B), LSTRVAL(C)
#  define SWIG_LUA_CONSTTAB_CHAR(B, C) LSTRKEY(B), LNUMVAL(C)
#else /* SWIG_LUA_FLAVOR_LUA */
#  define SWIG_LUA_CONSTTAB_INT(B, C) SWIG_LUA_INT, (char *)B, (long)C, 0, 0, 0
#  define SWIG_LUA_CONSTTAB_FLOAT(B, C) SWIG_LUA_FLOAT, (char *)B, 0, (double)C, 0, 0
#  define SWIG_LUA_CONSTTAB_STRING(B, C) SWIG_LUA_STRING, (char *)B, 0, 0, (void *)C, 0
#  define SWIG_LUA_CONSTTAB_CHAR(B, C) SWIG_LUA_CHAR, (char *)B, (long)C, 0, 0, 0
#endif

#if (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC)
#  define LRO_STRVAL(v) {{.p = (char *) v}, LUA_TSTRING}
#  define LSTRVAL LRO_STRVAL
#endif

/* -----------------------------------------------------------------------------
 * compatibility defines
 * ----------------------------------------------------------------------------- */

/* History of Lua C API length functions:  In Lua 5.0 (and before?)
   there was "lua_strlen".  In Lua 5.1, this was renamed "lua_objlen",
   but a compatibility define of "lua_strlen" was added.  In Lua 5.2,
   this function was again renamed, to "lua_rawlen" (to emphasize that
   it doesn't call the "__len" metamethod), and the compatibility
   define of lua_strlen was removed.  All SWIG uses have been updated
   to "lua_rawlen", and we add our own defines of that here for older
   versions of Lua.  */
#if !defined(LUA_VERSION_NUM) || LUA_VERSION_NUM < 501
# define lua_rawlen lua_strlen
#elif LUA_VERSION_NUM == 501
# define lua_rawlen lua_objlen
#endif


/* lua_pushglobaltable is the recommended "future-proof" way to get
   the global table for Lua 5.2 and later.  Here we define
   lua_pushglobaltable ourselves for Lua versions before 5.2.  */
#if !defined(LUA_VERSION_NUM) || LUA_VERSION_NUM < 502
# define lua_pushglobaltable(L) lua_pushvalue(L, LUA_GLOBALSINDEX)
#endif


/* -----------------------------------------------------------------------------
 * global swig types
 * ----------------------------------------------------------------------------- */
/* Constant table */
#define SWIG_LUA_INT     1
#define SWIG_LUA_FLOAT   2
#define SWIG_LUA_STRING  3
#define SWIG_LUA_POINTER 4
#define SWIG_LUA_BINARY  5
#define SWIG_LUA_CHAR    6

/* Structure for variable linking table */
typedef struct {
  const char *name;
  lua_CFunction get;
  lua_CFunction set;
} swig_lua_var_info;

/* Constant information structure */
typedef struct {
    int type;
    char *name;
    long lvalue;
    double dvalue;
    void   *pvalue;
    swig_type_info **ptype;
} swig_lua_const_info;

typedef struct {
  const char     *name;
  lua_CFunction   method;
} swig_lua_method;

typedef struct {
  const char     *name;
  lua_CFunction   getmethod;
  lua_CFunction   setmethod;
} swig_lua_attribute;

typedef struct swig_lua_class {
  const char    *name;
  swig_type_info   **type;
  lua_CFunction  constructor;
  void    (*destructor)(void *);
  swig_lua_method   *methods;
  swig_lua_attribute     *attributes;
  struct swig_lua_class **bases;
  const char **base_names;
} swig_lua_class;

/* this is the struct for wrappering all pointers in SwigLua
*/
typedef struct {
  swig_type_info   *type;
  int     own;  /* 1 if owned & must be destroyed */
  void        *ptr;
} swig_lua_userdata;

/* this is the struct for wrapping arbitary packed binary data
(currently it is only used for member function pointers)
the data ordering is similar to swig_lua_userdata, but it is currently not possible
to tell the two structures apart within SWIG, other than by looking at the type
*/
typedef struct {
  swig_type_info   *type;
  int     own;  /* 1 if owned & must be destroyed */
  char data[1];       /* arbitary amount of data */    
} swig_lua_rawdata;

/* Common SWIG API */
#define SWIG_NewPointerObj(L, ptr, type, owner)       SWIG_Lua_NewPointerObj(L, (void *)ptr, type, owner)
#define SWIG_ConvertPtr(L,idx, ptr, type, flags)    SWIG_Lua_ConvertPtr(L,idx,ptr,type,flags)
#define SWIG_MustGetPtr(L,idx, type,flags, argnum,fnname)  SWIG_Lua_MustGetPtr(L,idx, type,flags, argnum,fnname)
/* for C++ member pointers, ie, member methods */
#define SWIG_ConvertMember(L, idx, ptr, sz, ty)       SWIG_Lua_ConvertPacked(L, idx, ptr, sz, ty)
#define SWIG_NewMemberObj(L, ptr, sz, type)      SWIG_Lua_NewPackedObj(L, ptr, sz, type)

/* Runtime API */
#define SWIG_GetModule(clientdata) SWIG_Lua_GetModule((lua_State*)(clientdata))
#define SWIG_SetModule(clientdata, pointer) SWIG_Lua_SetModule((lua_State*) (clientdata), pointer)
#define SWIG_MODULE_CLIENTDATA_TYPE lua_State*

/* Contract support */
#define SWIG_contract_assert(expr, msg)  \
  if (!(expr)) { lua_pushstring(L, (char *) msg); goto fail; } else

/* helper #defines */
#define SWIG_fail {goto fail;}
#define SWIG_fail_arg(func_name,argnum,type) \
  {lua_pushfstring(L,"Error in %s (arg %d), expected '%s' got '%s'",\
  func_name,argnum,type,SWIG_Lua_typename(L,argnum));\
  goto fail;}
#define SWIG_fail_ptr(func_name,argnum,type) \
  SWIG_fail_arg(func_name,argnum,(type && type->str)?type->str:"void*")
#define SWIG_check_num_args(func_name,a,b) \
  if (lua_gettop(L)<a || lua_gettop(L)>b) \
  {lua_pushfstring(L,"Error in %s expected %d..%d args, got %d",func_name,a,b,lua_gettop(L));\
  goto fail;}


#define SWIG_Lua_get_table(L,n) \
  (lua_pushstring(L, n), lua_rawget(L,-2))

#define SWIG_Lua_add_function(L,n,f) \
  (lua_pushstring(L, n), \
      lua_pushcfunction(L, f), \
      lua_rawset(L,-3))

/* special helper for allowing 'nil' for usertypes */
#define SWIG_isptrtype(L,I) (lua_isuserdata(L,I) || lua_isnil(L,I))

#ifdef __cplusplus
/* Special helper for member function pointers 
it gets the address, casts it, then dereferences it */
//#define SWIG_mem_fn_as_voidptr(a)  (*((char**)&(a)))
#endif

/* storing/access of swig_module_info */
SWIGRUNTIME swig_module_info *
SWIG_Lua_GetModule(lua_State* L) {
  swig_module_info *ret = 0;
  lua_pushstring(L,"swig_runtime_data_type_pointer" SWIG_RUNTIME_VERSION SWIG_TYPE_TABLE_NAME);
  lua_rawget(L,LUA_REGISTRYINDEX);
  if (lua_islightuserdata(L,-1))
    ret=(swig_module_info*)lua_touserdata(L,-1);
  lua_pop(L,1);  /* tidy */
  return ret;
}

SWIGRUNTIME void
SWIG_Lua_SetModule(lua_State* L, swig_module_info *module) {
  /* add this all into the Lua registry: */
  lua_pushstring(L,"swig_runtime_data_type_pointer" SWIG_RUNTIME_VERSION SWIG_TYPE_TABLE_NAME);
  lua_pushlightuserdata(L,(void*)module);
  lua_rawset(L,LUA_REGISTRYINDEX);
}

/* -----------------------------------------------------------------------------
 * global variable support code: modules
 * ----------------------------------------------------------------------------- */

/* this function is called when trying to set an immutable.
default value is to print an error.
This can removed with a compile flag SWIGLUA_IGNORE_SET_IMMUTABLE */
SWIGINTERN int SWIG_Lua_set_immutable(lua_State* L)
{
/*  there should be 1 param passed in: the new value */
#ifndef SWIGLUA_IGNORE_SET_IMMUTABLE
  lua_pop(L,1);  /* remove it */
  lua_pushstring(L,"This variable is immutable");
  lua_error(L);
#endif
    return 0;   /* should not return anything */
}

/* the module.get method used for getting linked data */
SWIGINTERN int SWIG_Lua_module_get(lua_State* L)
{
/*  there should be 2 params passed in
  (1) table (not the meta table)
  (2) string name of the attribute
  printf("SWIG_Lua_module_get %p(%s) '%s'\n",
   lua_topointer(L,1),lua_typename(L,lua_type(L,1)),
   lua_tostring(L,2));
*/
  /* get the metatable */
#if ((SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC))
  assert(lua_isrotable(L,1)); /* just in case */
#else
  assert(lua_istable(L,1)); /* default Lua action */
#endif
  lua_getmetatable(L,1);  /* get the metatable */
#if ((SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC))
  assert(lua_isrotable(L,-1));  /* just in case */
#else
  assert(lua_istable(L,-1));
#endif
  SWIG_Lua_get_table(L,".get");  /* get the .get table */
  lua_remove(L,3);  /* remove metatable */
#if ((SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC))
  if (lua_isrotable(L,-1))
#else
  if (lua_istable(L,-1))
#endif
  {
    /* look for the key in the .get table */
    lua_pushvalue(L,2);  /* key */
    lua_rawget(L,-2);
    lua_remove(L,3);  /* remove .get */
    if (lua_iscfunction(L,-1))
    {  /* found it so call the fn & return its value */
      lua_call(L,0,1);
      return 1;
    }
    lua_pop(L,1);  /* remove the top */
  }
  lua_pop(L,1);  /* remove the .get */
  lua_pushnil(L);  /* return a nil */
  return 1;
}

/* the module.set method used for setting linked data */
SWIGINTERN int SWIG_Lua_module_set(lua_State* L)
{
/*  there should be 3 params passed in
  (1) table (not the meta table)
  (2) string name of the attribute
  (3) any for the new value
*/
  /* get the metatable */
#if ((SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC))
  assert(lua_isrotable(L,1));  /* just in case */
#else
  assert(lua_istable(L,1)); /* default Lua action */
#endif
  lua_getmetatable(L,1);  /* get the metatable */
#if ((SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC))
  assert(lua_isrotable(L,-1));  /* just in case */
#else
  assert(lua_istable(L,-1));
#endif
  SWIG_Lua_get_table(L,".set");  /* get the .set table */
  lua_remove(L,4);  /* remove metatable */
#if ((SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC))
  if (lua_isrotable(L,-1))
#else
  if (lua_istable(L,-1))
#endif
  {
    /* look for the key in the .set table */
    lua_pushvalue(L,2);  /* key */
    lua_rawget(L,-2);
    lua_remove(L,4);  /* remove .set */
    if (lua_iscfunction(L,-1))
    {  /* found it so call the fn & return its value */
      lua_pushvalue(L,3);  /* value */
      lua_call(L,1,0);
      return 0;
    }
#if (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) 
    else {
      return 0; // Exits stoically if an invalid key is initialized.
    }
#endif
  }
  lua_settop(L,3);  /* reset back to start */
  /* we now have the table, key & new value, so just set directly */
  lua_rawset(L,1);  /* add direct */
  return 0;
}

#if ((SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUA) && (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC))
/* registering a module in lua. Pushes the module table on the stack. */
SWIGINTERN void  SWIG_Lua_module_begin(lua_State* L,const char* name)
{
  assert(lua_istable(L,-1));  /* just in case */
  lua_pushstring(L,name);
  lua_newtable(L);   /* the table */
  /* add meta table */
  lua_newtable(L);    /* the meta table */
  SWIG_Lua_add_function(L,"__index",SWIG_Lua_module_get);
  SWIG_Lua_add_function(L,"__newindex",SWIG_Lua_module_set);
  lua_pushstring(L,".get");
  lua_newtable(L);    /* the .get table */
  lua_rawset(L,-3);  /* add .get into metatable */
  lua_pushstring(L,".set");
  lua_newtable(L);    /* the .set table */
  lua_rawset(L,-3);  /* add .set into metatable */
  lua_setmetatable(L,-2);  /* sets meta table in module */
#ifdef SWIG_LUA_MODULE_GLOBAL
  /* If requested, install the module directly into the global namespace. */
  lua_rawset(L,-3);        /* add module into parent */
  SWIG_Lua_get_table(L,name);   /* get the table back out */
#else
  /* Do not install the module table as global name. The stack top has
     the module table with the name below. We pop the top and replace
     the name with it. */
  lua_replace(L,-2);
#endif
}

/* ending the register */
SWIGINTERN void  SWIG_Lua_module_end(lua_State* L)
{
  lua_pop(L,1);       /* tidy stack (remove module) */
}

/* adding a linked variable to the module */
SWIGINTERN void SWIG_Lua_module_add_variable(lua_State* L,const char* name,lua_CFunction getFn,lua_CFunction setFn)
{
  assert(lua_istable(L,-1));  /* just in case */
  lua_getmetatable(L,-1);  /* get the metatable */
  assert(lua_istable(L,-1));  /* just in case */
  SWIG_Lua_get_table(L,".get"); /* find the .get table */
  assert(lua_istable(L,-1));  /* should be a table: */
  SWIG_Lua_add_function(L,name,getFn);
  lua_pop(L,1);       /* tidy stack (remove table) */
  if (setFn)  /* if there is a set fn */
  {
    SWIG_Lua_get_table(L,".set"); /* find the .set table */
    assert(lua_istable(L,-1));  /* should be a table: */
    SWIG_Lua_add_function(L,name,setFn);
    lua_pop(L,1);       /* tidy stack (remove table) */
  }
  lua_pop(L,1);       /* tidy stack (remove meta) */
}
#endif

/* adding a function module */
SWIGINTERN void  SWIG_Lua_module_add_function(lua_State* L,const char* name,lua_CFunction fn)
{
  SWIG_Lua_add_function(L,name,fn);
}

/* -----------------------------------------------------------------------------
 * global variable support code: classes
 * ----------------------------------------------------------------------------- */

/* the class.get method, performs the lookup of class attributes */
SWIGINTERN int  SWIG_Lua_class_get(lua_State* L)
{
/*  there should be 2 params passed in
  (1) userdata (not the meta table)
  (2) string name of the attribute
*/
  assert(lua_isuserdata(L,-2));  /* just in case */
  lua_getmetatable(L,-2);    /* get the meta table */
  assert(lua_istable(L,-1));  /* just in case */
  SWIG_Lua_get_table(L,".get"); /* find the .get table */
  assert(lua_istable(L,-1));  /* just in case */
  /* look for the key in the .get table */
  lua_pushvalue(L,2);  /* key */
  lua_rawget(L,-2);
  lua_remove(L,-2); /* stack tidy, remove .get table */
  if (lua_iscfunction(L,-1))
  {  /* found it so call the fn & return its value */
    lua_pushvalue(L,1);  /* the userdata */
    lua_call(L,1,1);  /* 1 value in (userdata),1 out (result) */
    lua_remove(L,-2); /* stack tidy, remove metatable */
    return 1;
  }
  lua_pop(L,1);  /* remove whatever was there */
  /* ok, so try the .fn table */
  SWIG_Lua_get_table(L,".fn"); /* find the .get table */
  assert(lua_istable(L,-1));  /* just in case */
  lua_pushvalue(L,2);  /* key */
  lua_rawget(L,-2);  /* look for the fn */
  lua_remove(L,-2); /* stack tidy, remove .fn table */
  if (lua_isfunction(L,-1)) /* note: if its a C function or lua function */
  {  /* found it so return the fn & let lua call it */
    lua_remove(L,-2); /* stack tidy, remove metatable */
    return 1;
  }
  lua_pop(L,1);  /* remove whatever was there */
  /* NEW: looks for the __getitem() fn
  this is a user provided get fn */
  SWIG_Lua_get_table(L,"__getitem"); /* find the __getitem fn */
  if (lua_iscfunction(L,-1))  /* if its there */
  {  /* found it so call the fn & return its value */
    lua_pushvalue(L,1);  /* the userdata */
    lua_pushvalue(L,2);  /* the parameter */
    lua_call(L,2,1);  /* 2 value in (userdata),1 out (result) */
    lua_remove(L,-2); /* stack tidy, remove metatable */
    return 1;
  }
  return 0;  /* sorry not known */
}

/* the class.set method, performs the lookup of class attributes */
SWIGINTERN int  SWIG_Lua_class_set(lua_State* L)
{
/*  there should be 3 params passed in
  (1) table (not the meta table)
  (2) string name of the attribute
  (3) any for the new value
printf("SWIG_Lua_class_set %p(%s) '%s' %p(%s)\n",
      lua_topointer(L,1),lua_typename(L,lua_type(L,1)),
      lua_tostring(L,2),
      lua_topointer(L,3),lua_typename(L,lua_type(L,3)));*/

  assert(lua_isuserdata(L,1));  /* just in case */
  lua_getmetatable(L,1);    /* get the meta table */
  assert(lua_istable(L,-1));  /* just in case */

  SWIG_Lua_get_table(L,".set"); /* find the .set table */
  if (lua_istable(L,-1))
  {
    /* look for the key in the .set table */
    lua_pushvalue(L,2);  /* key */
    lua_rawget(L,-2);
    if (lua_iscfunction(L,-1))
    {  /* found it so call the fn & return its value */
      lua_pushvalue(L,1);  /* userdata */
      lua_pushvalue(L,3);  /* value */
      lua_call(L,2,0);
      return 0;
    }
    lua_pop(L,1);  /* remove the value */
  }
  lua_pop(L,1);  /* remove the value .set table */
  /* NEW: looks for the __setitem() fn
  this is a user provided set fn */
  SWIG_Lua_get_table(L,"__setitem"); /* find the fn */
  if (lua_iscfunction(L,-1))  /* if its there */
  {  /* found it so call the fn & return its value */
    lua_pushvalue(L,1);  /* the userdata */
    lua_pushvalue(L,2);  /* the parameter */
    lua_pushvalue(L,3);  /* the value */
    lua_call(L,3,0);  /* 3 values in ,0 out */
    lua_remove(L,-2); /* stack tidy, remove metatable */
    return 1;
  }
  return 0;
}

/* the class.destruct method called by the interpreter */
SWIGINTERN int  SWIG_Lua_class_destruct(lua_State* L)
{
/*  there should be 1 params passed in
  (1) userdata (not the meta table) */
  swig_lua_userdata* usr;
  swig_lua_class* clss;
  assert(lua_isuserdata(L,-1));  /* just in case */
  usr=(swig_lua_userdata*)lua_touserdata(L,-1);  /* get it */
  /* if must be destroyed & has a destructor */
  if (usr->own) /* if must be destroyed */
  {
    clss=(swig_lua_class*)usr->type->clientdata;  /* get the class */
    if (clss && clss->destructor)  /* there is a destroy fn */
    {
      clss->destructor(usr->ptr);  /* bye bye */
    }
  }
  return 0;
}

/* the class.__tostring method called by the interpreter and print */
SWIGINTERN int  SWIG_Lua_class_tostring(lua_State* L)
{
/*  there should be 1 param passed in
  (1) userdata (not the metatable) */
  assert(lua_isuserdata(L,1));  /* just in case */
  unsigned long userData = (unsigned long)lua_touserdata(L,1); /* get the userdata address for later */
  lua_getmetatable(L,1);    /* get the meta table */
  assert(lua_istable(L,-1));  /* just in case */
  
  lua_getfield(L, -1, ".type");
  const char* className = lua_tostring(L, -1);
  
  char output[256];
  sprintf(output, "<%s userdata: %lX>", className, userData);
  
  lua_pushstring(L, (const char*)output);
  return 1;
}

/* to manually disown some userdata */
SWIGINTERN int  SWIG_Lua_class_disown(lua_State* L)
{
/*  there should be 1 params passed in
  (1) userdata (not the meta table) */
  swig_lua_userdata* usr;
  assert(lua_isuserdata(L,-1));  /* just in case */
  usr=(swig_lua_userdata*)lua_touserdata(L,-1);  /* get it */
  
  usr->own = 0; /* clear our ownership */
  return 0;
}

/* gets the swig class registry (or creates it) */
SWIGINTERN void  SWIG_Lua_get_class_registry(lua_State* L)
{
  /* add this all into the swig registry: */
  lua_pushstring(L,"SWIG");
  lua_rawget(L,LUA_REGISTRYINDEX);  /* get the registry */
  if (!lua_istable(L,-1))  /* not there */
  {  /* must be first time, so add it */
    lua_pop(L,1);  /* remove the result */
    lua_pushstring(L,"SWIG");
    lua_newtable(L);
    lua_rawset(L,LUA_REGISTRYINDEX);
    /* then get it */
    lua_pushstring(L,"SWIG");
    lua_rawget(L,LUA_REGISTRYINDEX);
  }
}

/* helper fn to get the classes metatable from the register */
SWIGINTERN void  SWIG_Lua_get_class_metatable(lua_State* L,const char* cname)
{
  SWIG_Lua_get_class_registry(L);  /* get the registry */
  lua_pushstring(L,cname);  /* get the name */
  lua_rawget(L,-2);    /* get it */
  lua_remove(L,-2);    /* tidy up (remove registry) */
}

/* helper add a variable to a registered class */
SWIGINTERN void  SWIG_Lua_add_class_variable(lua_State* L,const char* name,lua_CFunction getFn,lua_CFunction setFn)
{
  assert(lua_istable(L,-1));  /* just in case */
  SWIG_Lua_get_table(L,".get"); /* find the .get table */
  assert(lua_istable(L,-1));  /* just in case */
  SWIG_Lua_add_function(L,name,getFn);
  lua_pop(L,1);       /* tidy stack (remove table) */
  if (setFn)
  {
    SWIG_Lua_get_table(L,".set"); /* find the .set table */
    assert(lua_istable(L,-1));  /* just in case */
    SWIG_Lua_add_function(L,name,setFn);
    lua_pop(L,1);       /* tidy stack (remove table) */
  }
}

/* helper to recursively add class details (attributes & operations) */
SWIGINTERN void  SWIG_Lua_add_class_details(lua_State* L,swig_lua_class* clss)
{
  int i;
  /* call all the base classes first: we can then override these later: */
  for(i=0;clss->bases[i];i++)
  {
    SWIG_Lua_add_class_details(L,clss->bases[i]);
  }
  /* add fns */
  for(i=0;clss->attributes[i].name;i++){
    SWIG_Lua_add_class_variable(L,clss->attributes[i].name,clss->attributes[i].getmethod,clss->attributes[i].setmethod);
  }
  /* add methods to the metatable */
  SWIG_Lua_get_table(L,".fn"); /* find the .fn table */
  assert(lua_istable(L,-1));  /* just in case */
  for(i=0;clss->methods[i].name;i++){
    SWIG_Lua_add_function(L,clss->methods[i].name,clss->methods[i].method);
  }
  lua_pop(L,1);       /* tidy stack (remove table) */
  /*   add operator overloads
    these look ANY method which start with "__" and assume they
    are operator overloads & add them to the metatable
    (this might mess up is someone defines a method __gc (the destructor)*/
  for(i=0;clss->methods[i].name;i++){
    if (clss->methods[i].name[0]=='_' && clss->methods[i].name[1]=='_'){
      SWIG_Lua_add_function(L,clss->methods[i].name,clss->methods[i].method);
    }
  }
}

/* set up the base classes pointers.
Each class structure has a list of pointers to the base class structures.
This function fills them.
It cannot be done at compile time, as this will not work with hireachies
spread over more than one swig file. 
Therefore it must be done at runtime, querying the SWIG type system.
*/
SWIGINTERN void SWIG_Lua_init_base_class(lua_State* L,swig_lua_class* clss)
{
  int i=0;
  swig_module_info* module=SWIG_GetModule(L);
  for(i=0;clss->base_names[i];i++)
  {
    if (clss->bases[i]==0) /* not found yet */
    {
      /* lookup and cache the base class */
      swig_type_info *info = SWIG_TypeQueryModule(module,module,clss->base_names[i]);
      if (info) clss->bases[i] = (swig_lua_class *) info->clientdata;
    }
  }	
}

/* performs the entire class registration process */
SWIGINTERN void  SWIG_Lua_class_register(lua_State* L,swig_lua_class* clss)
{
  /*  add its constructor to module with the name of the class
  so you can do MyClass(...) as well as new_MyClass(...)
  BUT only if a constructor is defined
  (this overcomes the problem of pure virtual classes without constructors)*/
  if (clss->constructor)
    SWIG_Lua_add_function(L,clss->name,clss->constructor);

  SWIG_Lua_get_class_registry(L);  /* get the registry */
  lua_pushstring(L,clss->name);  /* get the name */
  lua_newtable(L);    /* create the metatable */
  /* add string of class name called ".type" */
  lua_pushstring(L,".type");
  lua_pushstring(L,clss->name);
  lua_rawset(L,-3);
  /* add a table called ".get" */
  lua_pushstring(L,".get");
  lua_newtable(L);
  lua_rawset(L,-3);
  /* add a table called ".set" */
  lua_pushstring(L,".set");
  lua_newtable(L);
  lua_rawset(L,-3);
  /* add a table called ".fn" */
  lua_pushstring(L,".fn");
  lua_newtable(L);
  /* add manual disown method */
  SWIG_Lua_add_function(L,"__disown",SWIG_Lua_class_disown);
  lua_rawset(L,-3);
  /* add accessor fns for using the .get,.set&.fn */
  SWIG_Lua_add_function(L,"__index",SWIG_Lua_class_get);
  SWIG_Lua_add_function(L,"__newindex",SWIG_Lua_class_set);
  SWIG_Lua_add_function(L,"__gc",SWIG_Lua_class_destruct);
  /* add tostring method for better output */
  SWIG_Lua_add_function(L,"__tostring",SWIG_Lua_class_tostring);
  /* add it */
  lua_rawset(L,-3);  /* metatable into registry */
  lua_pop(L,1);      /* tidy stack (remove registry) */

  SWIG_Lua_get_class_metatable(L,clss->name);
  SWIG_Lua_add_class_details(L,clss);  /* recursive adding of details (atts & ops) */
  lua_pop(L,1);      /* tidy stack (remove class metatable) */
}

/* -----------------------------------------------------------------------------
 * Class/structure conversion fns
 * ----------------------------------------------------------------------------- */

/* helper to add metatable to new lua object */
SWIGINTERN void _SWIG_Lua_AddMetatable(lua_State* L,swig_type_info *type)
{
  if (type->clientdata)  /* there is clientdata: so add the metatable */
  {
    SWIG_Lua_get_class_metatable(L,((swig_lua_class*)(type->clientdata))->name);
    if (lua_istable(L,-1))
    {
      lua_setmetatable(L,-2);
    }
    else
    {
      lua_pop(L,1);
    }
  }
}

/* pushes a new object into the lua stack */
SWIGRUNTIME void SWIG_Lua_NewPointerObj(lua_State* L,void* ptr,swig_type_info *type, int own)
{
  swig_lua_userdata* usr;
  if (!ptr){
    lua_pushnil(L);
    return;
  }
  usr=(swig_lua_userdata*)lua_newuserdata(L,sizeof(swig_lua_userdata));  /* get data */
  usr->ptr=ptr;  /* set the ptr */
  usr->type=type;
  usr->own=own;
#if (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC)
  _SWIG_Lua_AddMetatable(L,type); /* add metatable */
#endif
}

/* takes a object from the lua stack & converts it into an object of the correct type
 (if possible) */
SWIGRUNTIME int  SWIG_Lua_ConvertPtr(lua_State* L,int index,void** ptr,swig_type_info *type,int flags)
{
  swig_lua_userdata* usr;
  swig_cast_info *cast;
  if (lua_isnil(L,index)){*ptr=0; return SWIG_OK;}    /* special case: lua nil => NULL pointer */
  usr=(swig_lua_userdata*)lua_touserdata(L,index);  /* get data */
  if (usr)
  {
    if (flags & SWIG_POINTER_DISOWN) /* must disown the object */
    {
        usr->own=0;
    }
    if (!type)            /* special cast void*, no casting fn */
    {
      *ptr=usr->ptr;
      return SWIG_OK; /* ok */
    }
    cast=SWIG_TypeCheckStruct(usr->type,type); /* performs normal type checking */
    if (cast)
    {
      int newmemory = 0;
      *ptr=SWIG_TypeCast(cast,usr->ptr,&newmemory);
      assert(!newmemory); /* newmemory handling not yet implemented */
      return SWIG_OK;  /* ok */
    }
  }
  return SWIG_ERROR;  /* error */
}

SWIGRUNTIME void* SWIG_Lua_MustGetPtr(lua_State* L,int index,swig_type_info *type,int flags,
       int argnum,const char* func_name){
  void* result;
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,index,&result,type,flags))){
    lua_pushfstring(L,"Error in %s, expected a %s at argument number %d\n",
      func_name,(type && type->str)?type->str:"void*",argnum);
    lua_error(L);
  }
  return result;
}

/* pushes a packed userdata. user for member fn pointers only */
SWIGRUNTIME void SWIG_Lua_NewPackedObj(lua_State* L,void* ptr,size_t size,swig_type_info *type)
{
  swig_lua_rawdata* raw;
  assert(ptr); /* not acceptable to pass in a NULL value */
  raw=(swig_lua_rawdata*)lua_newuserdata(L,sizeof(swig_lua_rawdata)-1+size);  /* alloc data */
  raw->type=type;
  raw->own=0;
  memcpy(raw->data,ptr,size); /* copy the data */
  _SWIG_Lua_AddMetatable(L,type); /* add metatable */
}
    
/* converts a packed userdata. user for member fn pointers only */
SWIGRUNTIME int  SWIG_Lua_ConvertPacked(lua_State* L,int index,void* ptr,size_t size,swig_type_info *type)
{
  swig_lua_rawdata* raw;
  raw=(swig_lua_rawdata*)lua_touserdata(L,index);  /* get data */
  if (!raw) return SWIG_ERROR;  /* error */
  if (type==0 || type==raw->type) /* void* or identical type */
  {
    memcpy(ptr,raw->data,size); /* copy it */
    return SWIG_OK; /* ok */
  }
  return SWIG_ERROR;  /* error */
}

/* a function to get the typestring of a piece of data */
SWIGRUNTIME const char *SWIG_Lua_typename(lua_State *L, int tp)
{
  swig_lua_userdata* usr;
  if (lua_isuserdata(L,tp))
  {
    usr=(swig_lua_userdata*)lua_touserdata(L,tp);  /* get data */
    if (usr && usr->type && usr->type->str)
      return usr->type->str;
    return "userdata (unknown type)";
  }
  return lua_typename(L,lua_type(L,tp));
}

/* lua callable function to get the userdata's type */
SWIGRUNTIME int SWIG_Lua_type(lua_State* L)
{
  lua_pushstring(L,SWIG_Lua_typename(L,1));
  return 1;
}

/* lua callable function to compare userdata's value
the issue is that two userdata may point to the same thing
but to lua, they are different objects */
SWIGRUNTIME int SWIG_Lua_equal(lua_State* L)
{
  int result;
  swig_lua_userdata *usr1,*usr2;
  if (!lua_isuserdata(L,1) || !lua_isuserdata(L,2))  /* just in case */
    return 0;  /* nil reply */
  usr1=(swig_lua_userdata*)lua_touserdata(L,1);  /* get data */
  usr2=(swig_lua_userdata*)lua_touserdata(L,2);  /* get data */
  /*result=(usr1->ptr==usr2->ptr && usr1->type==usr2->type); only works if type is the same*/
  result=(usr1->ptr==usr2->ptr);
   lua_pushboolean(L,result);
  return 1;
}

/* -----------------------------------------------------------------------------
 * global variable support code: class/struct typemap functions
 * ----------------------------------------------------------------------------- */

#if ((SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUA) && (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC))
/* Install Constants */
SWIGINTERN void
SWIG_Lua_InstallConstants(lua_State* L, swig_lua_const_info constants[]) {
  int i;
  for (i = 0; constants[i].type; i++) {
    switch(constants[i].type) {
    case SWIG_LUA_INT:
      lua_pushstring(L,constants[i].name);
      lua_pushnumber(L,(lua_Number)constants[i].lvalue);
      lua_rawset(L,-3);
      break;
    case SWIG_LUA_FLOAT:
      lua_pushstring(L,constants[i].name);
      lua_pushnumber(L,(lua_Number)constants[i].dvalue);
      lua_rawset(L,-3);
      break;
    case SWIG_LUA_CHAR:
      lua_pushstring(L,constants[i].name);
      lua_pushfstring(L,"%c",(char)constants[i].lvalue);
      lua_rawset(L,-3);
      break;
    case SWIG_LUA_STRING:
      lua_pushstring(L,constants[i].name);
      lua_pushstring(L,(char *) constants[i].pvalue);
      lua_rawset(L,-3);
      break;
    case SWIG_LUA_POINTER:
      lua_pushstring(L,constants[i].name);
      SWIG_NewPointerObj(L,constants[i].pvalue, *(constants[i]).ptype,0);
      lua_rawset(L,-3);
      break;
    case SWIG_LUA_BINARY:
      lua_pushstring(L,constants[i].name);
      SWIG_NewMemberObj(L,constants[i].pvalue,constants[i].lvalue,*(constants[i]).ptype);
      lua_rawset(L,-3);
      break;
    default:
      break;
    }
  }
}
#endif

/* -----------------------------------------------------------------------------
 * executing lua code from within the wrapper
 * ----------------------------------------------------------------------------- */

#ifndef SWIG_DOSTRING_FAIL /* Allows redefining of error function */
#define SWIG_DOSTRING_FAIL(S) fprintf(stderr,"%s\n",S)
#endif
/* Executes a C string in Lua a really simple way of calling lua from C
Unfortunately lua keeps changing its API's, so we need a conditional compile
In lua 5.0.X its lua_dostring()
In lua 5.1.X its luaL_dostring()
*/
SWIGINTERN int 
SWIG_Lua_dostring(lua_State *L, const char* str) {
  int ok,top;
  if (str==0 || str[0]==0) return 0; /* nothing to do */
  top=lua_gettop(L); /* save stack */
#if (defined(LUA_VERSION_NUM) && (LUA_VERSION_NUM>=501))
  ok=luaL_dostring(L,str);	/* looks like this is lua 5.1.X or later, good */
#else
  ok=lua_dostring(L,str);	/* might be lua 5.0.x, using lua_dostring */
#endif
  if (ok!=0) {
    SWIG_DOSTRING_FAIL(lua_tostring(L,-1));
  }
  lua_settop(L,top); /* restore the stack */
  return ok;
}    

#ifdef __cplusplus
}
#endif

/* ------------------------------ end luarun.swg  ------------------------------ */


/* -------- TYPES TABLE (BEGIN) -------- */

#define SWIGTYPE_p_ipv4 swig_types[0]
#define SWIGTYPE_p_ipv4_addr swig_types[1]
#define SWIGTYPE_p_ipv4_flags swig_types[2]
#define SWIGTYPE_p_ipv4_network swig_types[3]
#define SWIGTYPE_p_ipv4_payload swig_types[4]
#define SWIGTYPE_p_packet swig_types[5]
static swig_type_info *swig_types[7];
static swig_module_info swig_module = {swig_types, 6, 0, 0, 0, 0};
#define SWIG_TypeQuery(name) SWIG_TypeQueryModule(&swig_module, &swig_module, name)
#define SWIG_MangledTypeQuery(name) SWIG_MangledTypeQueryModule(&swig_module, &swig_module, name)

/* -------- TYPES TABLE (END) -------- */

#define SWIG_name      "ipv4"
#define SWIG_init      luaopen_ipv4
#define SWIG_init_user luaopen_ipv4_user

#define SWIG_LUACODE   luaopen_ipv4_luacode

	#include "haka/ipv4.h"

	struct ipv4_flags;
	struct ipv4_payload;

	struct ipv4_addr {
		ipv4addr   addr;
	};

	struct ipv4_network {
		ipv4network  net;
	};

	struct ipv4_addr *ipv4_addr_new(ipv4addr a)
	{
		struct ipv4_addr *ret = malloc(sizeof(struct ipv4_addr));
		if (!ret) {
			return NULL;
		}

		ret->addr = a;
		return ret;
	}

	//SWIGEXPORT int luaopen_protocol_ipv4(lua_State* L) __attribute__ ((alias ("luaopen_ipv4")));


	#include <haka/error.h>
	#include <haka/lua/lua.h>

	#include <stdlib.h>

	typedef const char *temporary_string;

	/* GCC 4.7 has a problem with some code generated by SWIG and trigger
	 * a warning. We need to disable it in order to compile properly. */
	#pragma GCC diagnostic ignored "-Warray-bounds"

	#undef SWIG_fail
	#undef SWIG_fail_arg
	#undef SWIG_check_num_args

	#define SWIG_fail {SWIG_arg=-1;goto fail;}
	#define SWIG_fail_arg(func_name,argnum,type) \
	  {lua_pushfstring(L,"Error in %s (arg %d), expected '%s' got '%s'",\
	  func_name,argnum,type,SWIG_Lua_typename(L,argnum));\
	  SWIG_arg=-1;goto fail;}
	#define SWIG_check_num_args(func_name,a,b) \
	  if (lua_gettop(L)<a || lua_gettop(L)>b) \
	  {lua_pushfstring(L,"Error in %s expected %d..%d args, got %d",func_name,a,b,lua_gettop(L));\
	  SWIG_arg=-1;goto fail;}
	#define SWIG_execfail {SWIG_arg=-2;goto fail;}

	#define SWIG_lua_always(L, a) 1


#ifdef __cplusplus	/* generic alloc/dealloc fns*/
#define SWIG_ALLOC_ARRAY(TYPE,LEN) 	new TYPE[LEN]
#define SWIG_FREE_ARRAY(PTR)		delete[] PTR
#else
#define SWIG_ALLOC_ARRAY(TYPE,LEN) 	(TYPE *)malloc(LEN*sizeof(TYPE))
#define SWIG_FREE_ARRAY(PTR)		free(PTR)
#endif
/* counting the size of arrays:*/
SWIGINTERN int SWIG_itable_size(lua_State* L, int index)
{
	int n=0;
	while(1){
		lua_rawgeti(L,index,n+1);
		if (lua_isnil(L,-1))break;
		++n;
		lua_pop(L,1);
	}
	lua_pop(L,1);
	return n;
}

SWIGINTERN int SWIG_table_size(lua_State* L, int index)
{
	int n=0;
	lua_pushnil(L);  /* first key*/
	while (lua_next(L, index) != 0) {
		++n;
		lua_pop(L, 1);  /* removes `value'; keeps `key' for next iteration*/
	}
	return n;
}

/* super macro to declare array typemap helper fns */
#define SWIG_DECLARE_TYPEMAP_ARR_FN(NAME,TYPE)\
	SWIGINTERN int SWIG_read_##NAME##_num_array(lua_State* L,int index,TYPE *array,int size){\
		int i;\
		for (i = 0; i < size; i++) {\
			lua_rawgeti(L,index,i+1);\
			if (lua_isnumber(L,-1)){\
				array[i] = (TYPE)lua_tonumber(L,-1);\
			} else {\
				lua_pop(L,1);\
				return 0;\
			}\
			lua_pop(L,1);\
		}\
		return 1;\
	}\
	SWIGINTERN TYPE* SWIG_get_##NAME##_num_array_fixed(lua_State* L, int index, int size){\
		TYPE *array;\
		if (!lua_istable(L,index) || SWIG_itable_size(L,index) != size) {\
			lua_pushfstring(L,"expected a table of size %d",size);\
			return 0;\
		}\
		array=SWIG_ALLOC_ARRAY(TYPE,size);\
		if (!SWIG_read_##NAME##_num_array(L,index,array,size)){\
			lua_pushstring(L,"table must contain numbers");\
			SWIG_FREE_ARRAY(array);\
			return 0;\
		}\
		return array;\
	}\
	SWIGINTERN TYPE* SWIG_get_##NAME##_num_array_var(lua_State* L, int index, int* size)\
	{\
		TYPE *array;\
		if (!lua_istable(L,index)) {\
			lua_pushstring(L,"expected a table");\
			return 0;\
		}\
		*size=SWIG_itable_size(L,index);\
		if (*size<1){\
			lua_pushstring(L,"table appears to be empty");\
			return 0;\
		}\
		array=SWIG_ALLOC_ARRAY(TYPE,*size);\
		if (!SWIG_read_##NAME##_num_array(L,index,array,*size)){\
			lua_pushstring(L,"table must contain numbers");\
			SWIG_FREE_ARRAY(array);\
			return 0;\
		}\
		return array;\
	}\
	SWIGINTERN void SWIG_write_##NAME##_num_array(lua_State* L,TYPE *array,int size){\
		int i;\
		lua_newtable(L);\
		for (i = 0; i < size; i++){\
			lua_pushnumber(L,(lua_Number)array[i]);\
			lua_rawseti(L,-2,i+1);/* -1 is the number, -2 is the table*/ \
		}\
	}

SWIG_DECLARE_TYPEMAP_ARR_FN(schar,signed char);
SWIG_DECLARE_TYPEMAP_ARR_FN(uchar,unsigned char);
SWIG_DECLARE_TYPEMAP_ARR_FN(int,int);
SWIG_DECLARE_TYPEMAP_ARR_FN(uint,unsigned int);
SWIG_DECLARE_TYPEMAP_ARR_FN(short,short);
SWIG_DECLARE_TYPEMAP_ARR_FN(ushort,unsigned short);
SWIG_DECLARE_TYPEMAP_ARR_FN(long,long);
SWIG_DECLARE_TYPEMAP_ARR_FN(ulong,unsigned long);
SWIG_DECLARE_TYPEMAP_ARR_FN(float,float);
SWIG_DECLARE_TYPEMAP_ARR_FN(double,double);

SWIGINTERN int SWIG_read_ptr_array(lua_State* L,int index,void **array,int size,swig_type_info *type){
	int i;
	for (i = 0; i < size; i++) {
		lua_rawgeti(L,index,i+1);
		if (!lua_isuserdata(L,-1) || SWIG_ConvertPtr(L,-1,&array[i],type,0)==-1){
			lua_pop(L,1);
			return 0;
		}
		lua_pop(L,1);
	}
	return 1;
}
SWIGINTERN void** SWIG_get_ptr_array_fixed(lua_State* L, int index, int size,swig_type_info *type){
	void **array;
	if (!lua_istable(L,index) || SWIG_itable_size(L,index) != size) {
		lua_pushfstring(L,"expected a table of size %d",size);
		return 0;
	}
	array=SWIG_ALLOC_ARRAY(void*,size);
	if (!SWIG_read_ptr_array(L,index,array,size,type)){
		lua_pushfstring(L,"table must contain pointers of type %s",type->name);
		SWIG_FREE_ARRAY(array);
		return 0;
	}
	return array;
}
SWIGINTERN void** SWIG_get_ptr_array_var(lua_State* L, int index, int* size,swig_type_info *type){
	void **array;
	if (!lua_istable(L,index)) {
		lua_pushstring(L,"expected a table");
		return 0;
	}
	*size=SWIG_itable_size(L,index);
	if (*size<1){
		lua_pushstring(L,"table appears to be empty");
		return 0;
	}
	array=SWIG_ALLOC_ARRAY(void*,*size);
	if (!SWIG_read_ptr_array(L,index,array,*size,type)){
		lua_pushfstring(L,"table must contain pointers of type %s",type->name);
		SWIG_FREE_ARRAY(array);
		return 0;
	}
	return array;
}
SWIGINTERN void SWIG_write_ptr_array(lua_State* L,void **array,int size,swig_type_info *type,int own){
	int i;
	lua_newtable(L);
	for (i = 0; i < size; i++){
		SWIG_NewPointerObj(L,array[i],type,own);
		lua_rawseti(L,-2,i+1);/* -1 is the number, -2 is the table*/
	}
}


#include <haka/lua/object.h>
#include <haka/config.h>

extern bool lua_object_push(lua_State *L, void *ptr, struct lua_object *obj, swig_type_info *type_info, int owner);


#include "haka/ipv4.h"



struct ipv4_addr;
struct ipv4_addr *ipv4_addr_new(ipv4addr a);



SWIGINTERN int SWIG_lua_isnilstring(lua_State *L, int idx) {
  int ret = lua_isstring(L, idx);
  if (!ret)
   ret = lua_isnil(L, idx);
  return ret;
}

SWIGINTERN struct ipv4_addr *new_ipv4_addr__SWIG_0(char const *str){
			struct ipv4_addr *ret = malloc(sizeof(struct ipv4_addr));
			if (!ret) {
				return NULL;
			}

			ret->addr = ipv4_addr_from_string(str);
			if (check_error()) {
				free(ret);
				return NULL;
			}

			return ret;
		}
SWIGINTERN struct ipv4_addr *new_ipv4_addr__SWIG_1(unsigned int addr){
			return ipv4_addr_new(addr);
		}
SWIGINTERN struct ipv4_addr *new_ipv4_addr__SWIG_2(unsigned int a,unsigned int b,unsigned int c,unsigned int d){
			struct ipv4_addr *ret = malloc(sizeof(struct ipv4_addr));
			if (!ret) {
				return NULL;
			}

			ret->addr = ipv4_addr_from_bytes(a, b, c, d);
			if (check_error()) {
				free(ret);
				return NULL;
			}

			return ret;
		}
SWIGINTERN void delete_ipv4_addr(struct ipv4_addr *self){
			if (self)
				free(self);
		}
SWIGINTERN bool ipv4_addr___eq(struct ipv4_addr const *self,struct ipv4_addr *addr){
			return self->addr == addr->addr;
		}
SWIGINTERN bool ipv4_addr___lt(struct ipv4_addr const *self,struct ipv4_addr *addr){
			return self->addr < addr->addr;
		}
SWIGINTERN bool ipv4_addr___le(struct ipv4_addr const *self,struct ipv4_addr *addr){
			return self->addr <= addr->addr;
		}
SWIGINTERN temporary_string ipv4_addr___tostring(struct ipv4_addr *self){
			char *buffer = malloc(IPV4_ADDR_STRING_MAXLEN + 1);
			if (!buffer) {
				error(L"memory error");
				return NULL;
			}
			ipv4_addr_to_string(self->addr, buffer, IPV4_ADDR_STRING_MAXLEN + 1);
			return buffer;
		}

void ipv4_addr___getitem(struct ipv4_addr *self, char *KEY) {
	error(L"unknown field '%s'", KEY);
}

void ipv4_addr___setitem(struct ipv4_addr *self, char *KEY, void *ANY) {
	error(L"unknown field '%s'", KEY);
}

SWIGINTERN struct ipv4_network *new_ipv4_network__SWIG_0(char const *str){
			struct ipv4_network *ret = malloc(sizeof(struct ipv4_network));
			if (!ret) {
				return NULL;
			}

			ret->net = ipv4_network_from_string(str);
			if (check_error()) {
				free(ret);
				return NULL;
			}

			return ret;
		}
SWIGINTERN struct ipv4_network *new_ipv4_network__SWIG_1(struct ipv4_addr addr,unsigned char mask){
			if (mask < 0 || mask > 32) {
				error(L"Invalid IPv4 addresss network format");
				return NULL;
			}

			struct ipv4_network *ret = malloc(sizeof(struct ipv4_network));
			if (!ret) {
				return NULL;
			}
			ret->net.net = addr.addr;
			ret->net.mask = mask;
			return ret;
		}
SWIGINTERN void delete_ipv4_network(struct ipv4_network *self){
			if (self)
				free(self);
		}
SWIGINTERN temporary_string ipv4_network___tostring(struct ipv4_network *self){
			char *buffer = malloc(IPV4_NETWORK_STRING_MAXLEN + 1);
			if (!buffer) {
				error(L"memory error");
				return NULL;
			}
			ipv4_network_to_string(self->net, buffer,
					IPV4_NETWORK_STRING_MAXLEN + 1);

			return buffer;
		}
SWIGINTERN bool ipv4_network__contains(struct ipv4_network *self,struct ipv4_addr *addr){
			if (!addr) {
				error(L"nil argument");
				return false;
			}
			return ipv4_network_contains(self->net, addr->addr);
		}

void ipv4_network___getitem(struct ipv4_network *self, char *KEY) {
	error(L"unknown field '%s'", KEY);
}

void ipv4_network___setitem(struct ipv4_network *self, char *KEY, void *ANY) {
	error(L"unknown field '%s'", KEY);
}


void ipv4_flags___getitem(struct ipv4_flags *self, char *KEY) {
	error(L"unknown field '%s'", KEY);
}

void ipv4_flags___setitem(struct ipv4_flags *self, char *KEY, void *ANY) {
	error(L"unknown field '%s'", KEY);
}

SWIGINTERN size_t ipv4_payload___len(struct ipv4_payload *self,void *dummy){
			return ipv4_get_payload_length((struct ipv4 *)self);
		}
SWIGINTERN int ipv4_payload___getitem__SWIG_0(struct ipv4_payload *self,int index){
			const size_t size = ipv4_get_payload_length((struct ipv4 *)self);

			--index;
			if (index < 0 || index >= size) {
				error(L"out-of-bound index");
				return 0;
			}
			return ipv4_get_payload((struct ipv4 *)self)[index];
		}
SWIGINTERN void ipv4_payload___setitem__SWIG_0(struct ipv4_payload *self,int index,int value){
			const size_t size = ipv4_get_payload_length((struct ipv4 *)self);

			--index;
			if (index < 0 || index >= size) {
				error(L"out-of-bound index");
				return;
			}
			ipv4_get_payload_modifiable((struct ipv4 *)self)[index] = value;
		}
SWIGINTERN void ipv4_payload_resize(struct ipv4_payload *self,int size){
			ipv4_resize_payload((struct ipv4 *)self, size);
		}

void ipv4_payload___getitem(struct ipv4_payload *self, char *KEY) {
	error(L"unknown field '%s'", KEY);
}

void ipv4_payload___setitem(struct ipv4_payload *self, char *KEY, void *ANY) {
	error(L"unknown field '%s'", KEY);
}

SWIGINTERN void delete_ipv4(struct ipv4 *self){
			if (self)
				ipv4_release(self);
		}
SWIGINTERN void ipv4_drop(struct ipv4 *self){
			ipv4_action_drop(self);
		}
SWIGINTERN void ipv4_send(struct ipv4 *self){
			ipv4_action_send(self);
		}

void ipv4___getitem(struct ipv4 *self, char *KEY) {
	error(L"unknown field '%s'", KEY);
}

void ipv4___setitem(struct ipv4 *self, char *KEY, void *ANY) {
	error(L"unknown field '%s'", KEY);
}



	#define IPV4_INT_GETSET(field) \
		unsigned int ipv4_##field##_get(struct ipv4 *ip) { return ipv4_get_##field(ip); } \
		void ipv4_##field##_set(struct ipv4 *ip, unsigned int v) { ipv4_set_##field(ip, v); }

	IPV4_INT_GETSET(version);
	IPV4_INT_GETSET(hdr_len);
	IPV4_INT_GETSET(tos);
	IPV4_INT_GETSET(len);
	IPV4_INT_GETSET(id);
	IPV4_INT_GETSET(frag_offset);
	IPV4_INT_GETSET(ttl);
	IPV4_INT_GETSET(proto);
	IPV4_INT_GETSET(checksum);

	struct packet *ipv4_raw_get(struct ipv4 *ip) { return ip->packet; }

	struct ipv4_addr *ipv4_src_get(struct ipv4 *ip) { return ipv4_addr_new(ipv4_get_src(ip)); }
	void ipv4_src_set(struct ipv4 *ip, struct ipv4_addr *v) { ipv4_set_src(ip, v->addr); }
	struct ipv4_addr *ipv4_dst_get(struct ipv4 *ip) { return ipv4_addr_new(ipv4_get_dst(ip)); }
	void ipv4_dst_set(struct ipv4 *ip, struct ipv4_addr *v) { ipv4_set_dst(ip, v->addr); }

	struct ipv4_payload *ipv4_payload_get(struct ipv4 *ip) { return (struct ipv4_payload *)ip; }

	const char *ipv4_dissector_get(struct ipv4 *ip) { return "ipv4"; }

	const char *ipv4_next_dissector_get(struct ipv4 *ip) { return ipv4_get_proto_dissector(ip); }

	struct ipv4_flags *ipv4_flags_get(struct ipv4 *ip) { return (struct ipv4_flags *)ip; }

	#define IPV4_FLAGS_GETSET(field) \
		bool ipv4_flags_##field##_get(struct ipv4_flags *flags) { return ipv4_get_flags_##field((struct ipv4 *)flags); } \
		void ipv4_flags_##field##_set(struct ipv4_flags *flags, bool v) { return ipv4_set_flags_##field((struct ipv4 *)flags, v); }

	IPV4_FLAGS_GETSET(rb);
	IPV4_FLAGS_GETSET(df);
	IPV4_FLAGS_GETSET(mf);

	unsigned int ipv4_flags_all_get(struct ipv4_flags *flags) { return ipv4_get_flags((struct ipv4 *)flags); }
	void ipv4_flags_all_set(struct ipv4_flags *flags, unsigned int v) { return ipv4_set_flags((struct ipv4 *)flags, v); }


	struct ipv4_addr *ipv4_network_net_get(struct ipv4_network *network) { return ipv4_addr_new(network->net.net); }

	unsigned char ipv4_network_mask_get(struct ipv4_network *network) { return network->net.mask; }


#ifdef __cplusplus
extern "C" {
#endif
static int _wrap_new_addr__SWIG_0(lua_State* L) {
  int SWIG_arg = 0;
  char *arg1 = (char *) 0 ;
  struct ipv4_addr *result = 0 ;
  
  SWIG_check_num_args("ipv4_addr::ipv4_addr",1,1)
  if(!SWIG_lua_isnilstring(L,1)) SWIG_fail_arg("ipv4_addr::ipv4_addr",1,"char const *");
  arg1 = (char *)lua_tostring(L, 1);
  
  const wchar_t *error;
  result = (struct ipv4_addr *)new_ipv4_addr__SWIG_0((char const *)arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_addr,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_new_addr__SWIG_1(lua_State* L) {
  int SWIG_arg = 0;
  unsigned int arg1 ;
  struct ipv4_addr *result = 0 ;
  
  SWIG_check_num_args("ipv4_addr::ipv4_addr",1,1)
  if(!lua_isnumber(L,1)) SWIG_fail_arg("ipv4_addr::ipv4_addr",1,"unsigned int");
  SWIG_contract_assert((lua_tonumber(L,1)>=0),"number must not be negative")
  arg1 = (unsigned int)lua_tonumber(L, 1);
  
  const wchar_t *error;
  result = (struct ipv4_addr *)new_ipv4_addr__SWIG_1(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_addr,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_new_addr__SWIG_2(lua_State* L) {
  int SWIG_arg = 0;
  unsigned int arg1 ;
  unsigned int arg2 ;
  unsigned int arg3 ;
  unsigned int arg4 ;
  struct ipv4_addr *result = 0 ;
  
  SWIG_check_num_args("ipv4_addr::ipv4_addr",4,4)
  if(!lua_isnumber(L,1)) SWIG_fail_arg("ipv4_addr::ipv4_addr",1,"unsigned int");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4_addr::ipv4_addr",2,"unsigned int");
  if(!lua_isnumber(L,3)) SWIG_fail_arg("ipv4_addr::ipv4_addr",3,"unsigned int");
  if(!lua_isnumber(L,4)) SWIG_fail_arg("ipv4_addr::ipv4_addr",4,"unsigned int");
  SWIG_contract_assert((lua_tonumber(L,1)>=0),"number must not be negative")
  arg1 = (unsigned int)lua_tonumber(L, 1);
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  SWIG_contract_assert((lua_tonumber(L,3)>=0),"number must not be negative")
  arg3 = (unsigned int)lua_tonumber(L, 3);
  SWIG_contract_assert((lua_tonumber(L,4)>=0),"number must not be negative")
  arg4 = (unsigned int)lua_tonumber(L, 4);
  
  const wchar_t *error;
  result = (struct ipv4_addr *)new_ipv4_addr__SWIG_2(arg1,arg2,arg3,arg4);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_addr,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_new_addr(lua_State* L) {
  int argc;
  int argv[5]={
    1,2,3,4,5
  };
  
  argc = lua_gettop(L);
  if (argc == 1) {
    int _v;
    {
      _v = lua_isnumber(L,argv[0]);
    }
    if (_v) {
      return _wrap_new_addr__SWIG_1(L);
    }
  }
  if (argc == 1) {
    int _v;
    {
      _v = SWIG_lua_isnilstring(L,argv[0]);
    }
    if (_v) {
      return _wrap_new_addr__SWIG_0(L);
    }
  }
  if (argc == 4) {
    int _v;
    {
      _v = lua_isnumber(L,argv[0]);
    }
    if (_v) {
      {
        _v = lua_isnumber(L,argv[1]);
      }
      if (_v) {
        {
          _v = lua_isnumber(L,argv[2]);
        }
        if (_v) {
          {
            _v = lua_isnumber(L,argv[3]);
          }
          if (_v) {
            return _wrap_new_addr__SWIG_2(L);
          }
        }
      }
    }
  }
  
  lua_pushstring(L,"Wrong arguments for overloaded function 'new_addr'\n"
    "  Possible C/C++ prototypes are:\n"
    "    ipv4_addr::ipv4_addr(char const *)\n"
    "    ipv4_addr::ipv4_addr(unsigned int)\n"
    "    ipv4_addr::ipv4_addr(unsigned int,unsigned int,unsigned int,unsigned int)\n");
  lua_error(L);return 0;
}


static int _wrap_addr___eq(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_addr *arg1 = (struct ipv4_addr *) 0 ;
  struct ipv4_addr *arg2 = (struct ipv4_addr *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4_addr::__eq",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_addr::__eq",1,"struct ipv4_addr const *");
  if(!SWIG_isptrtype(L,2)) SWIG_fail_arg("ipv4_addr::__eq",2,"struct ipv4_addr *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___eq", 1, SWIGTYPE_p_ipv4_addr);
  }
  
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 2, (void**)&arg2, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___eq", 2, SWIGTYPE_p_ipv4_addr);
  }
  
  
  const wchar_t *error;
  result = (bool)ipv4_addr___eq((struct ipv4_addr const *)arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_addr___lt(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_addr *arg1 = (struct ipv4_addr *) 0 ;
  struct ipv4_addr *arg2 = (struct ipv4_addr *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4_addr::__lt",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_addr::__lt",1,"struct ipv4_addr const *");
  if(!SWIG_isptrtype(L,2)) SWIG_fail_arg("ipv4_addr::__lt",2,"struct ipv4_addr *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___lt", 1, SWIGTYPE_p_ipv4_addr);
  }
  
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 2, (void**)&arg2, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___lt", 2, SWIGTYPE_p_ipv4_addr);
  }
  
  
  const wchar_t *error;
  result = (bool)ipv4_addr___lt((struct ipv4_addr const *)arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_addr___le(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_addr *arg1 = (struct ipv4_addr *) 0 ;
  struct ipv4_addr *arg2 = (struct ipv4_addr *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4_addr::__le",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_addr::__le",1,"struct ipv4_addr const *");
  if(!SWIG_isptrtype(L,2)) SWIG_fail_arg("ipv4_addr::__le",2,"struct ipv4_addr *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___le", 1, SWIGTYPE_p_ipv4_addr);
  }
  
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 2, (void**)&arg2, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___le", 2, SWIGTYPE_p_ipv4_addr);
  }
  
  
  const wchar_t *error;
  result = (bool)ipv4_addr___le((struct ipv4_addr const *)arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_addr___tostring(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_addr *arg1 = (struct ipv4_addr *) 0 ;
  temporary_string result;
  
  SWIG_check_num_args("ipv4_addr::__tostring",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_addr::__tostring",1,"struct ipv4_addr *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___tostring", 1, SWIGTYPE_p_ipv4_addr);
  }
  
  
  const wchar_t *error;
  result = ipv4_addr___tostring(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    lua_pushstring(L, (const char *)result); SWIG_arg++;
    free((void*)result);
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_addr___getitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_addr *arg1 = (struct ipv4_addr *) 0 ;
  char *arg2 = (char *) 0 ;
  
  SWIG_check_num_args("ipv4_addr::__getitem",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_addr::__getitem",1,"struct ipv4_addr *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___getitem", 1, SWIGTYPE_p_ipv4_addr);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  
  const wchar_t *error;
  ipv4_addr___getitem(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_addr___setitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_addr *arg1 = (struct ipv4_addr *) 0 ;
  char *arg2 = (char *) 0 ;
  void *arg3 = (void *) 0 ;
  
  SWIG_check_num_args("ipv4_addr::__setitem",3,3)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_addr::__setitem",1,"struct ipv4_addr *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("addr___setitem", 1, SWIGTYPE_p_ipv4_addr);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    arg3 = (void *)lua_topointer(L, 3);
  }
  
  const wchar_t *error;
  ipv4_addr___setitem(arg1,arg2,arg3);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static void swig_delete_addr(void *obj) {
struct ipv4_addr *arg1 = (struct ipv4_addr *) obj;
delete_ipv4_addr(arg1);
}
static swig_lua_method swig_ipv4_addr_methods[] = {
    {"__eq", _wrap_addr___eq}, 
    {"__lt", _wrap_addr___lt}, 
    {"__le", _wrap_addr___le}, 
    {"__tostring", _wrap_addr___tostring}, 
    {"__getitem", _wrap_addr___getitem}, 
    {"__setitem", _wrap_addr___setitem}, 
    {0,0}
};
static swig_lua_attribute swig_ipv4_addr_attributes[] = {
    {0,0,0}
};
static swig_lua_class *swig_ipv4_addr_bases[] = {0};
static const char *swig_ipv4_addr_base_names[] = {0};
static swig_lua_class _wrap_class_ipv4_addr = { "addr", &SWIGTYPE_p_ipv4_addr,_wrap_new_addr, swig_delete_addr, swig_ipv4_addr_methods, swig_ipv4_addr_attributes, swig_ipv4_addr_bases, swig_ipv4_addr_base_names };

static int _wrap_new_network__SWIG_0(lua_State* L) {
  int SWIG_arg = 0;
  char *arg1 = (char *) 0 ;
  struct ipv4_network *result = 0 ;
  
  SWIG_check_num_args("ipv4_network::ipv4_network",1,1)
  if(!SWIG_lua_isnilstring(L,1)) SWIG_fail_arg("ipv4_network::ipv4_network",1,"char const *");
  arg1 = (char *)lua_tostring(L, 1);
  
  const wchar_t *error;
  result = (struct ipv4_network *)new_ipv4_network__SWIG_0((char const *)arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_network,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_new_network__SWIG_1(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_addr arg1 ;
  unsigned char arg2 ;
  struct ipv4_addr *argp1 ;
  struct ipv4_network *result = 0 ;
  
  SWIG_check_num_args("ipv4_network::ipv4_network",2,2)
  if(!lua_isuserdata(L,1)) SWIG_fail_arg("ipv4_network::ipv4_network",1,"struct ipv4_addr");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4_network::ipv4_network",2,"unsigned char");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,1,(void**)&argp1,SWIGTYPE_p_ipv4_addr,0))){
    SWIG_fail_ptr("new_network",1,SWIGTYPE_p_ipv4_addr);
  }
  arg1 = *argp1;
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned char)lua_tonumber(L, 2);
  
  const wchar_t *error;
  result = (struct ipv4_network *)new_ipv4_network__SWIG_1(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_network,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_new_network(lua_State* L) {
  int argc;
  int argv[3]={
    1,2,3
  };
  
  argc = lua_gettop(L);
  if (argc == 1) {
    int _v;
    {
      _v = SWIG_lua_isnilstring(L,argv[0]);
    }
    if (_v) {
      return _wrap_new_network__SWIG_0(L);
    }
  }
  if (argc == 2) {
    int _v;
    {
      void *ptr;
      if (lua_isuserdata(L,argv[0])==0 || SWIG_ConvertPtr(L,argv[0], (void **) &ptr, SWIGTYPE_p_ipv4_addr, 0)) {
        _v = 0;
      } else {
        _v = 1;
      }
    }
    if (_v) {
      {
        _v = lua_isnumber(L,argv[1]);
      }
      if (_v) {
        return _wrap_new_network__SWIG_1(L);
      }
    }
  }
  
  lua_pushstring(L,"Wrong arguments for overloaded function 'new_network'\n"
    "  Possible C/C++ prototypes are:\n"
    "    ipv4_network::ipv4_network(char const *)\n"
    "    ipv4_network::ipv4_network(struct ipv4_addr,unsigned char)\n");
  lua_error(L);return 0;
}


static int _wrap_network___tostring(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_network *arg1 = (struct ipv4_network *) 0 ;
  temporary_string result;
  
  SWIG_check_num_args("ipv4_network::__tostring",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_network::__tostring",1,"struct ipv4_network *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,1,(void**)&arg1,SWIGTYPE_p_ipv4_network,0))){
    SWIG_fail_ptr("network___tostring",1,SWIGTYPE_p_ipv4_network);
  }
  
  
  const wchar_t *error;
  result = ipv4_network___tostring(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    lua_pushstring(L, (const char *)result); SWIG_arg++;
    free((void*)result);
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_network_contains(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_network *arg1 = (struct ipv4_network *) 0 ;
  struct ipv4_addr *arg2 = (struct ipv4_addr *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4_network::_contains",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_network::_contains",1,"struct ipv4_network *");
  if(!SWIG_isptrtype(L,2)) SWIG_fail_arg("ipv4_network::_contains",2,"struct ipv4_addr *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,1,(void**)&arg1,SWIGTYPE_p_ipv4_network,0))){
    SWIG_fail_ptr("network_contains",1,SWIGTYPE_p_ipv4_network);
  }
  
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 2, (void**)&arg2, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("network_contains", 2, SWIGTYPE_p_ipv4_addr);
  }
  
  
  const wchar_t *error;
  result = (bool)ipv4_network__contains(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_network_net_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_network *arg1 = (struct ipv4_network *) 0 ;
  struct ipv4_addr *result = 0 ;
  
  SWIG_check_num_args("ipv4_network::net",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_network::net",1,"struct ipv4_network *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,1,(void**)&arg1,SWIGTYPE_p_ipv4_network,0))){
    SWIG_fail_ptr("network_net_get",1,SWIGTYPE_p_ipv4_network);
  }
  
  
  const wchar_t *error;
  result = (struct ipv4_addr *)ipv4_network_net_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_addr,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_network_mask_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_network *arg1 = (struct ipv4_network *) 0 ;
  unsigned char result;
  
  SWIG_check_num_args("ipv4_network::mask",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_network::mask",1,"struct ipv4_network *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,1,(void**)&arg1,SWIGTYPE_p_ipv4_network,0))){
    SWIG_fail_ptr("network_mask_get",1,SWIGTYPE_p_ipv4_network);
  }
  
  
  const wchar_t *error;
  result = (unsigned char)ipv4_network_mask_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_network___getitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_network *arg1 = (struct ipv4_network *) 0 ;
  char *arg2 = (char *) 0 ;
  
  SWIG_check_num_args("ipv4_network::__getitem",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_network::__getitem",1,"struct ipv4_network *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,1,(void**)&arg1,SWIGTYPE_p_ipv4_network,0))){
    SWIG_fail_ptr("network___getitem",1,SWIGTYPE_p_ipv4_network);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  
  const wchar_t *error;
  ipv4_network___getitem(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_network___setitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_network *arg1 = (struct ipv4_network *) 0 ;
  char *arg2 = (char *) 0 ;
  void *arg3 = (void *) 0 ;
  
  SWIG_check_num_args("ipv4_network::__setitem",3,3)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_network::__setitem",1,"struct ipv4_network *");
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L,1,(void**)&arg1,SWIGTYPE_p_ipv4_network,0))){
    SWIG_fail_ptr("network___setitem",1,SWIGTYPE_p_ipv4_network);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    arg3 = (void *)lua_topointer(L, 3);
  }
  
  const wchar_t *error;
  ipv4_network___setitem(arg1,arg2,arg3);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static void swig_delete_network(void *obj) {
struct ipv4_network *arg1 = (struct ipv4_network *) obj;
delete_ipv4_network(arg1);
}
static swig_lua_method swig_ipv4_network_methods[] = {
    {"__tostring", _wrap_network___tostring}, 
    {"contains", _wrap_network_contains}, 
    {"__getitem", _wrap_network___getitem}, 
    {"__setitem", _wrap_network___setitem}, 
    {0,0}
};
static swig_lua_attribute swig_ipv4_network_attributes[] = {
    { "net", _wrap_network_net_get, SWIG_Lua_set_immutable},
    { "mask", _wrap_network_mask_get, SWIG_Lua_set_immutable},
    {0,0,0}
};
static swig_lua_class *swig_ipv4_network_bases[] = {0};
static const char *swig_ipv4_network_base_names[] = {0};
static swig_lua_class _wrap_class_ipv4_network = { "network", &SWIGTYPE_p_ipv4_network,_wrap_new_network, swig_delete_network, swig_ipv4_network_methods, swig_ipv4_network_attributes, swig_ipv4_network_bases, swig_ipv4_network_base_names };

static int _wrap_ipv4_flags_rb_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  bool arg2 ;
  
  SWIG_check_num_args("ipv4_flags::rb",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::rb",1,"struct ipv4_flags *");
  if(!lua_isboolean(L,2)) SWIG_fail_arg("ipv4_flags::rb",2,"bool");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_rb_set", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  arg2 = (lua_toboolean(L, 2)!=0);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_rb_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_flags_rb_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_rb_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4_flags::rb",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::rb",1,"struct ipv4_flags *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_rb_get", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_rb_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (bool)ipv4_flags_rb_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_df_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  bool arg2 ;
  
  SWIG_check_num_args("ipv4_flags::df",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::df",1,"struct ipv4_flags *");
  if(!lua_isboolean(L,2)) SWIG_fail_arg("ipv4_flags::df",2,"bool");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_df_set", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  arg2 = (lua_toboolean(L, 2)!=0);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_df_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_flags_df_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_df_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4_flags::df",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::df",1,"struct ipv4_flags *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_df_get", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_df_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (bool)ipv4_flags_df_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_mf_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  bool arg2 ;
  
  SWIG_check_num_args("ipv4_flags::mf",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::mf",1,"struct ipv4_flags *");
  if(!lua_isboolean(L,2)) SWIG_fail_arg("ipv4_flags::mf",2,"bool");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_mf_set", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  arg2 = (lua_toboolean(L, 2)!=0);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_mf_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_flags_mf_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_mf_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4_flags::mf",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::mf",1,"struct ipv4_flags *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_mf_get", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_mf_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (bool)ipv4_flags_mf_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_all_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4_flags::all",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::all",1,"struct ipv4_flags *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4_flags::all",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_all_set", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_all_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_flags_all_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_all_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4_flags::all",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::all",1,"struct ipv4_flags *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags_all_get", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_all_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_flags_all_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags___getitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  char *arg2 = (char *) 0 ;
  
  SWIG_check_num_args("ipv4_flags::__getitem",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::__getitem",1,"struct ipv4_flags *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags___getitem", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags___getitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_flags___getitem(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags___setitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_flags *arg1 = (struct ipv4_flags *) 0 ;
  char *arg2 = (char *) 0 ;
  void *arg3 = (void *) 0 ;
  
  SWIG_check_num_args("ipv4_flags::__setitem",3,3)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_flags::__setitem",1,"struct ipv4_flags *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_flags, 0))){
    SWIG_fail_ptr("ipv4_flags___setitem", 1, SWIGTYPE_p_ipv4_flags);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    arg3 = (void *)lua_topointer(L, 3);
  }
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags___setitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_flags___setitem(arg1,arg2,arg3);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static swig_lua_method swig_ipv4_flags_methods[] = {
    {"__getitem", _wrap_ipv4_flags___getitem}, 
    {"__setitem", _wrap_ipv4_flags___setitem}, 
    {0,0}
};
static swig_lua_attribute swig_ipv4_flags_attributes[] = {
    { "rb", _wrap_ipv4_flags_rb_get, _wrap_ipv4_flags_rb_set},
    { "df", _wrap_ipv4_flags_df_get, _wrap_ipv4_flags_df_set},
    { "mf", _wrap_ipv4_flags_mf_get, _wrap_ipv4_flags_mf_set},
    { "all", _wrap_ipv4_flags_all_get, _wrap_ipv4_flags_all_set},
    {0,0,0}
};
static swig_lua_class *swig_ipv4_flags_bases[] = {0};
static const char *swig_ipv4_flags_base_names[] = {0};
static swig_lua_class _wrap_class_ipv4_flags = { "ipv4_flags", &SWIGTYPE_p_ipv4_flags,0,0, swig_ipv4_flags_methods, swig_ipv4_flags_attributes, swig_ipv4_flags_bases, swig_ipv4_flags_base_names };

static int _wrap_ipv4_payload___len(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_payload *arg1 = (struct ipv4_payload *) 0 ;
  void *arg2 = (void *) 0 ;
  size_t result;
  
  SWIG_check_num_args("ipv4_payload::__len",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_payload::__len",1,"struct ipv4_payload *");
  if(!SWIG_isptrtype(L,2)) SWIG_fail_arg("ipv4_payload::__len",2,"void *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_payload, 0))){
    SWIG_fail_ptr("ipv4_payload___len", 1, SWIGTYPE_p_ipv4_payload);
  }
  
  arg2=(void *)SWIG_MustGetPtr(L,2,0,0,2,"ipv4_payload___len");
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_payload___len: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = ipv4_payload___len(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_payload___getitem__SWIG_0(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_payload *arg1 = (struct ipv4_payload *) 0 ;
  int arg2 ;
  int result;
  
  SWIG_check_num_args("ipv4_payload::__getitem",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_payload::__getitem",1,"struct ipv4_payload *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4_payload::__getitem",2,"int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_payload, 0))){
    SWIG_fail_ptr("ipv4_payload___getitem", 1, SWIGTYPE_p_ipv4_payload);
  }
  
  arg2 = (int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_payload___getitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (int)ipv4_payload___getitem__SWIG_0(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_payload___setitem__SWIG_0(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_payload *arg1 = (struct ipv4_payload *) 0 ;
  int arg2 ;
  int arg3 ;
  
  SWIG_check_num_args("ipv4_payload::__setitem",3,3)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_payload::__setitem",1,"struct ipv4_payload *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4_payload::__setitem",2,"int");
  if(!lua_isnumber(L,3)) SWIG_fail_arg("ipv4_payload::__setitem",3,"int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_payload, 0))){
    SWIG_fail_ptr("ipv4_payload___setitem", 1, SWIGTYPE_p_ipv4_payload);
  }
  
  arg2 = (int)lua_tonumber(L, 2);
  arg3 = (int)lua_tonumber(L, 3);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_payload___setitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_payload___setitem__SWIG_0(arg1,arg2,arg3);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_payload_resize(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_payload *arg1 = (struct ipv4_payload *) 0 ;
  int arg2 ;
  
  SWIG_check_num_args("ipv4_payload::resize",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_payload::resize",1,"struct ipv4_payload *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4_payload::resize",2,"int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_payload, 0))){
    SWIG_fail_ptr("ipv4_payload_resize", 1, SWIGTYPE_p_ipv4_payload);
  }
  
  arg2 = (int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_payload_resize: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_payload_resize(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_payload___getitem__SWIG_1(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_payload *arg1 = (struct ipv4_payload *) 0 ;
  char *arg2 = (char *) 0 ;
  
  SWIG_check_num_args("ipv4_payload::__getitem",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_payload::__getitem",1,"struct ipv4_payload *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_payload, 0))){
    SWIG_fail_ptr("ipv4_payload___getitem", 1, SWIGTYPE_p_ipv4_payload);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_payload___getitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_payload___getitem(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_payload___getitem(lua_State* L) {
  int argc;
  int argv[3]={
    1,2,3
  };
  
  argc = lua_gettop(L);
  if (argc == 2) {
    int _v;
    {
      void *ptr;
      if (SWIG_isptrtype(L,argv[0])==0 || SWIG_ConvertPtr(L,argv[0], (void **) &ptr, SWIGTYPE_p_ipv4_payload, 0)) {
        _v = 0;
      } else {
        _v = 1;
      }
    }
    if (_v) {
      {
        _v = lua_isnumber(L,argv[1]);
      }
      if (_v) {
        return _wrap_ipv4_payload___getitem__SWIG_0(L);
      }
    }
  }
  if (argc == 2) {
    int _v;
    {
      void *ptr;
      if (SWIG_isptrtype(L,argv[0])==0 || SWIG_ConvertPtr(L,argv[0], (void **) &ptr, SWIGTYPE_p_ipv4_payload, 0)) {
        _v = 0;
      } else {
        _v = 1;
      }
    }
    if (_v) {
      {
        _v = 1;
      }
      if (_v) {
        return _wrap_ipv4_payload___getitem__SWIG_1(L);
      }
    }
  }
  
  lua_pushstring(L,"Wrong arguments for overloaded function 'ipv4_payload___getitem'\n"
    "  Possible C/C++ prototypes are:\n"
    "    ipv4_payload::__getitem(int)\n"
    "    ipv4_payload::__getitem(char *)\n");
  lua_error(L);return 0;
}


static int _wrap_ipv4_payload___setitem__SWIG_1(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4_payload *arg1 = (struct ipv4_payload *) 0 ;
  char *arg2 = (char *) 0 ;
  void *arg3 = (void *) 0 ;
  
  SWIG_check_num_args("ipv4_payload::__setitem",3,3)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_payload::__setitem",1,"struct ipv4_payload *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4_payload, 0))){
    SWIG_fail_ptr("ipv4_payload___setitem", 1, SWIGTYPE_p_ipv4_payload);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    arg3 = (void *)lua_topointer(L, 3);
  }
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_payload___setitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_payload___setitem(arg1,arg2,arg3);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_payload___setitem(lua_State* L) {
  int argc;
  int argv[4]={
    1,2,3,4
  };
  
  argc = lua_gettop(L);
  if (argc == 3) {
    int _v;
    {
      void *ptr;
      if (SWIG_isptrtype(L,argv[0])==0 || SWIG_ConvertPtr(L,argv[0], (void **) &ptr, SWIGTYPE_p_ipv4_payload, 0)) {
        _v = 0;
      } else {
        _v = 1;
      }
    }
    if (_v) {
      {
        _v = lua_isnumber(L,argv[1]);
      }
      if (_v) {
        {
          _v = lua_isnumber(L,argv[2]);
        }
        if (_v) {
          return _wrap_ipv4_payload___setitem__SWIG_0(L);
        }
      }
    }
  }
  if (argc == 3) {
    int _v;
    {
      void *ptr;
      if (SWIG_isptrtype(L,argv[0])==0 || SWIG_ConvertPtr(L,argv[0], (void **) &ptr, SWIGTYPE_p_ipv4_payload, 0)) {
        _v = 0;
      } else {
        _v = 1;
      }
    }
    if (_v) {
      {
        _v = 1;
      }
      if (_v) {
        {
          _v = 1;
        }
        if (_v) {
          return _wrap_ipv4_payload___setitem__SWIG_1(L);
        }
      }
    }
  }
  
  lua_pushstring(L,"Wrong arguments for overloaded function 'ipv4_payload___setitem'\n"
    "  Possible C/C++ prototypes are:\n"
    "    ipv4_payload::__setitem(int,int)\n"
    "    ipv4_payload::__setitem(char *,void *)\n");
  lua_error(L);return 0;
}


static swig_lua_method swig_ipv4_payload_methods[] = {
    {"__len", _wrap_ipv4_payload___len}, 
    {"resize", _wrap_ipv4_payload_resize}, 
    {"__getitem", _wrap_ipv4_payload___getitem}, 
    {"__setitem", _wrap_ipv4_payload___setitem}, 
    {0,0}
};
static swig_lua_attribute swig_ipv4_payload_attributes[] = {
    {0,0,0}
};
static swig_lua_class *swig_ipv4_payload_bases[] = {0};
static const char *swig_ipv4_payload_base_names[] = {0};
static swig_lua_class _wrap_class_ipv4_payload = { "ipv4_payload", &SWIGTYPE_p_ipv4_payload,0,0, swig_ipv4_payload_methods, swig_ipv4_payload_attributes, swig_ipv4_payload_bases, swig_ipv4_payload_base_names };

static int _wrap_ipv4_hdr_len_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::hdr_len",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::hdr_len",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::hdr_len",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_hdr_len_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_hdr_len_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_hdr_len_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_hdr_len_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::hdr_len",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::hdr_len",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_hdr_len_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_hdr_len_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_hdr_len_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_version_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::version",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::version",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::version",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_version_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_version_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_version_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_version_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::version",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::version",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_version_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_version_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_version_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_tos_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::tos",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::tos",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::tos",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_tos_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_tos_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_tos_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_tos_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::tos",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::tos",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_tos_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_tos_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_tos_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_len_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::len",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::len",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::len",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_len_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_len_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_len_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_len_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::len",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::len",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_len_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_len_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_len_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_id_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::id",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::id",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::id",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_id_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_id_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_id_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_id_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::id",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::id",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_id_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_id_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_id_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_frag_offset_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::frag_offset",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::frag_offset",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::frag_offset",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_frag_offset_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_frag_offset_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_frag_offset_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_frag_offset_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::frag_offset",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::frag_offset",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_frag_offset_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_frag_offset_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_frag_offset_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_ttl_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::ttl",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::ttl",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::ttl",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_ttl_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_ttl_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_ttl_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_ttl_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::ttl",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::ttl",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_ttl_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_ttl_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_ttl_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_proto_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::proto",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::proto",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::proto",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_proto_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_proto_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_proto_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_proto_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::proto",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::proto",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_proto_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_proto_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_proto_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_checksum_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int arg2 ;
  
  SWIG_check_num_args("ipv4::checksum",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::checksum",1,"struct ipv4 *");
  if(!lua_isnumber(L,2)) SWIG_fail_arg("ipv4::checksum",2,"unsigned int");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_checksum_set", 1, SWIGTYPE_p_ipv4);
  }
  
  SWIG_contract_assert((lua_tonumber(L,2)>=0),"number must not be negative")
  arg2 = (unsigned int)lua_tonumber(L, 2);
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_checksum_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_checksum_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_checksum_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  unsigned int result;
  
  SWIG_check_num_args("ipv4::checksum",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::checksum",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_checksum_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_checksum_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (unsigned int)ipv4_checksum_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushnumber(L, (lua_Number) result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_src_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct ipv4_addr *arg2 = (struct ipv4_addr *) 0 ;
  
  SWIG_check_num_args("ipv4::src",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::src",1,"struct ipv4 *");
  if(!SWIG_isptrtype(L,2)) SWIG_fail_arg("ipv4::src",2,"struct ipv4_addr *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_src_set", 1, SWIGTYPE_p_ipv4);
  }
  
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 2, (void**)&arg2, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("ipv4_src_set", 2, SWIGTYPE_p_ipv4_addr);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_src_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_src_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_src_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct ipv4_addr *result = 0 ;
  
  SWIG_check_num_args("ipv4::src",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::src",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_src_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_src_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct ipv4_addr *)ipv4_src_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_addr,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_dst_set(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct ipv4_addr *arg2 = (struct ipv4_addr *) 0 ;
  
  SWIG_check_num_args("ipv4::dst",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::dst",1,"struct ipv4 *");
  if(!SWIG_isptrtype(L,2)) SWIG_fail_arg("ipv4::dst",2,"struct ipv4_addr *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_dst_set", 1, SWIGTYPE_p_ipv4);
  }
  
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 2, (void**)&arg2, SWIGTYPE_p_ipv4_addr, 0))){
    SWIG_fail_ptr("ipv4_dst_set", 2, SWIGTYPE_p_ipv4_addr);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_dst_set: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_dst_set(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_dst_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct ipv4_addr *result = 0 ;
  
  SWIG_check_num_args("ipv4::dst",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::dst",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_dst_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_dst_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct ipv4_addr *)ipv4_dst_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  SWIG_NewPointerObj(L,result,SWIGTYPE_p_ipv4_addr,1); SWIG_arg++; 
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_raw_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct packet *result = 0 ;
  
  SWIG_check_num_args("ipv4::raw",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::raw",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_raw_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_raw_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct packet *)ipv4_raw_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    if (!lua_object_push(L, result, &((struct packet*)result)->lua_object, SWIGTYPE_p_packet, 0)) {
      SWIG_execfail;
    }
    else {
      SWIG_arg++;
    }
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_flags_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct ipv4_flags *result = 0 ;
  
  SWIG_check_num_args("ipv4::flags",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::flags",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_flags_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_flags_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct ipv4_flags *)ipv4_flags_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    if (!lua_object_push(L, result, &((struct ipv4*)result)->lua_object, SWIGTYPE_p_ipv4_flags, 0)) {
      SWIG_execfail;
    }
    else {
      SWIG_arg++;
    }
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_payload_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct ipv4_payload *result = 0 ;
  
  SWIG_check_num_args("ipv4::payload",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::payload",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_payload_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_payload_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct ipv4_payload *)ipv4_payload_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    if (!lua_object_push(L, result, &((struct ipv4*)result)->lua_object, SWIGTYPE_p_ipv4_payload, 0)) {
      SWIG_execfail;
    }
    else {
      SWIG_arg++;
    }
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_dissector_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  char *result = 0 ;
  
  SWIG_check_num_args("ipv4::dissector",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::dissector",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_dissector_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_dissector_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (char *)ipv4_dissector_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushstring(L,(const char *)result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_next_dissector_get(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  char *result = 0 ;
  
  SWIG_check_num_args("ipv4::next_dissector",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::next_dissector",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_next_dissector_get", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_next_dissector_get: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (char *)ipv4_next_dissector_get(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushstring(L,(const char *)result); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_verify_checksum(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4::verify_checksum",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::verify_checksum",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_verify_checksum", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_verify_checksum: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (bool)ipv4_verify_checksum(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_compute_checksum(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  
  SWIG_check_num_args("ipv4::compute_checksum",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::compute_checksum",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_compute_checksum", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_compute_checksum: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_compute_checksum(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_drop(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  
  SWIG_check_num_args("ipv4::drop",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::drop",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_drop", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_drop: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_drop(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_send(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  
  SWIG_check_num_args("ipv4::send",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::send",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_send", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_send: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4_send(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_valid(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  bool result;
  
  SWIG_check_num_args("ipv4::valid",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::valid",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_valid", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_valid: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (bool)ipv4_valid(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  lua_pushboolean(L,(int)(result!=0)); SWIG_arg++;
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4_forge(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  struct packet *result = 0 ;
  
  SWIG_check_num_args("ipv4::forge",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::forge",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4_forge", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4_forge: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct packet *)ipv4_forge(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    if (!lua_object_push(L, result, &((struct packet*)result)->lua_object, SWIGTYPE_p_packet, 1)) {
      SWIG_execfail;
    }
    else {
      SWIG_arg++;
    }
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4___getitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  char *arg2 = (char *) 0 ;
  
  SWIG_check_num_args("ipv4::__getitem",2,2)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::__getitem",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4___getitem", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4___getitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4___getitem(arg1,arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_ipv4___setitem(lua_State* L) {
  int SWIG_arg = 0;
  struct ipv4 *arg1 = (struct ipv4 *) 0 ;
  char *arg2 = (char *) 0 ;
  void *arg3 = (void *) 0 ;
  
  SWIG_check_num_args("ipv4::__setitem",3,3)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4::__setitem",1,"struct ipv4 *");
  
#ifdef HAKA_DEBUG
  if (0) {
    if (!lua_isnil(L, 1)) {
      swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
      if (usr) {
        assert(!0 || usr->own == 1);
      }
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_ipv4, 0))){
    SWIG_fail_ptr("ipv4___setitem", 1, SWIGTYPE_p_ipv4);
  }
  
  {
    size_t len;
    arg2 = (char *)lua_converttostring(L, 2, &len);
  }
  {
    arg3 = (void *)lua_topointer(L, 3);
  }
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"ipv4___setitem: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  ipv4___setitem(arg1,arg2,arg3);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


static void swig_delete_ipv4(void *obj) {
struct ipv4 *arg1 = (struct ipv4 *) obj;
delete_ipv4(arg1);
}
static swig_lua_method swig_ipv4_methods[] = {
    {"verify_checksum", _wrap_ipv4_verify_checksum}, 
    {"compute_checksum", _wrap_ipv4_compute_checksum}, 
    {"drop", _wrap_ipv4_drop}, 
    {"send", _wrap_ipv4_send}, 
    {"valid", _wrap_ipv4_valid}, 
    {"forge", _wrap_ipv4_forge}, 
    {"__getitem", _wrap_ipv4___getitem}, 
    {"__setitem", _wrap_ipv4___setitem}, 
    {0,0}
};
static swig_lua_attribute swig_ipv4_attributes[] = {
    { "hdr_len", _wrap_ipv4_hdr_len_get, _wrap_ipv4_hdr_len_set},
    { "version", _wrap_ipv4_version_get, _wrap_ipv4_version_set},
    { "tos", _wrap_ipv4_tos_get, _wrap_ipv4_tos_set},
    { "len", _wrap_ipv4_len_get, _wrap_ipv4_len_set},
    { "id", _wrap_ipv4_id_get, _wrap_ipv4_id_set},
    { "frag_offset", _wrap_ipv4_frag_offset_get, _wrap_ipv4_frag_offset_set},
    { "ttl", _wrap_ipv4_ttl_get, _wrap_ipv4_ttl_set},
    { "proto", _wrap_ipv4_proto_get, _wrap_ipv4_proto_set},
    { "checksum", _wrap_ipv4_checksum_get, _wrap_ipv4_checksum_set},
    { "src", _wrap_ipv4_src_get, _wrap_ipv4_src_set},
    { "dst", _wrap_ipv4_dst_get, _wrap_ipv4_dst_set},
    { "raw", _wrap_ipv4_raw_get, SWIG_Lua_set_immutable},
    { "flags", _wrap_ipv4_flags_get, SWIG_Lua_set_immutable},
    { "payload", _wrap_ipv4_payload_get, SWIG_Lua_set_immutable},
    { "dissector", _wrap_ipv4_dissector_get, SWIG_Lua_set_immutable},
    { "next_dissector", _wrap_ipv4_next_dissector_get, SWIG_Lua_set_immutable},
    {0,0,0}
};
static swig_lua_class *swig_ipv4_bases[] = {0};
static const char *swig_ipv4_base_names[] = {0};
static swig_lua_class _wrap_class_ipv4 = { "ipv4", &SWIGTYPE_p_ipv4,0, swig_delete_ipv4, swig_ipv4_methods, swig_ipv4_attributes, swig_ipv4_bases, swig_ipv4_base_names };

static int _wrap_dissect(lua_State* L) {
  int SWIG_arg = 0;
  struct packet *arg1 = (struct packet *) 0 ;
  struct ipv4 *result = 0 ;
  
  SWIG_check_num_args("ipv4_dissect",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_dissect",1,"struct packet *");
  
#ifdef HAKA_DEBUG
  if (!lua_isnil(L, 1)) {
    swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
    if (usr) {
      assert(usr->own == 1);
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_packet, SWIG_POINTER_DISOWN))){
    SWIG_fail_ptr("dissect", 1, SWIGTYPE_p_packet);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"dissect: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct ipv4 *)ipv4_dissect(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    if (!lua_object_push(L, result, &((struct ipv4*)result)->lua_object, SWIGTYPE_p_ipv4, 1)) {
      SWIG_execfail;
    }
    else {
      SWIG_arg++;
    }
  }
  {
    if (SWIG_arg == -2 && arg1) {
      if (lua_object_push(L, arg1, &((struct packet*)arg1)->lua_object, SWIGTYPE_p_packet, 1)) {
        lua_pop(L, 1);
      }
    }
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  {
    if (SWIG_arg == -2 && arg1) {
      if (lua_object_push(L, arg1, &((struct packet*)arg1)->lua_object, SWIGTYPE_p_packet, 1)) {
        lua_pop(L, 1);
      }
    }
  }
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_create(lua_State* L) {
  int SWIG_arg = 0;
  struct packet *arg1 = (struct packet *) 0 ;
  struct ipv4 *result = 0 ;
  
  SWIG_check_num_args("ipv4_create",1,1)
  if(!SWIG_isptrtype(L,1)) SWIG_fail_arg("ipv4_create",1,"struct packet *");
  
#ifdef HAKA_DEBUG
  if (!lua_isnil(L, 1)) {
    swig_lua_userdata *usr = (swig_lua_userdata*)lua_touserdata(L, 1);
    if (usr) {
      assert(usr->own == 1);
    }
  }
#endif
  
  if (!SWIG_IsOK(SWIG_ConvertPtr(L, 1, (void**)&arg1, SWIGTYPE_p_packet, SWIG_POINTER_DISOWN))){
    SWIG_fail_ptr("create", 1, SWIGTYPE_p_packet);
  }
  
  {
    /* check that the object is still valid */
    if(!arg1) {
      lua_pushstring(L,"create: object has been destroyed");
      SWIG_fail;
    }
  }
  
  const wchar_t *error;
  result = (struct ipv4 *)ipv4_create(arg1);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  {
    if (!lua_object_push(L, result, &((struct ipv4*)result)->lua_object, SWIGTYPE_p_ipv4, 1)) {
      SWIG_execfail;
    }
    else {
      SWIG_arg++;
    }
  }
  {
    if (SWIG_arg == -2 && arg1) {
      if (lua_object_push(L, arg1, &((struct packet*)arg1)->lua_object, SWIGTYPE_p_packet, 1)) {
        lua_pop(L, 1);
      }
    }
  }
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  {
    if (SWIG_arg == -2 && arg1) {
      if (lua_object_push(L, arg1, &((struct packet*)arg1)->lua_object, SWIGTYPE_p_packet, 1)) {
        lua_pop(L, 1);
      }
    }
  }
  lua_error(L);
  return SWIG_arg;
}


static int _wrap_register_proto(lua_State* L) {
  int SWIG_arg = 0;
  int arg1 ;
  char *arg2 = (char *) 0 ;
  
  SWIG_check_num_args("ipv4_register_proto_dissector",2,2)
  if(!lua_isnumber(L,1)) SWIG_fail_arg("ipv4_register_proto_dissector",1,"int");
  if(!SWIG_lua_isnilstring(L,2)) SWIG_fail_arg("ipv4_register_proto_dissector",2,"char const *");
  arg1 = (int)lua_tonumber(L, 1);
  arg2 = (char *)lua_tostring(L, 2);
  
  const wchar_t *error;
  ipv4_register_proto_dissector(arg1,(char const *)arg2);
  if ((error = clear_error())) {
    int size;
    char *errormb;
    
    size = wcstombs(NULL, error, 0);
    if (size == (size_t)-1) {
      lua_pushstring(L, "unknown error");
      SWIG_execfail;
    }
    
    errormb = malloc(size+1);
    if (!errormb) {
      lua_pushstring(L, "memory error");
      SWIG_execfail;
    }
    
    wcstombs(errormb, error, size+1);
    lua_pushstring(L, errormb);
    free(errormb);
    
    SWIG_execfail;
  }
  
  
  return SWIG_arg;
  
  if(0) SWIG_fail;
  
fail:
  lua_error(L);
  return SWIG_arg;
}


#ifdef __cplusplus
}
#endif

static const struct luaL_Reg swig_commands[] = {
    { "dissect", _wrap_dissect},
    { "create", _wrap_create},
    { "register_proto", _wrap_register_proto},
    {0,0}
};

static swig_lua_var_info swig_variables[] = {
    {0,0,0}
};

static swig_lua_const_info swig_constants[] = {
    {0,0,0,0,0,0}
};

/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (BEGIN) -------- */

static swig_type_info _swigt__p_ipv4 = {"_p_ipv4", "struct ipv4 *|ipv4 *", 0, 0, (void*)&_wrap_class_ipv4, 0};
static swig_type_info _swigt__p_ipv4_addr = {"_p_ipv4_addr", "struct ipv4_addr *|ipv4_addr *", 0, 0, (void*)&_wrap_class_ipv4_addr, 0};
static swig_type_info _swigt__p_ipv4_flags = {"_p_ipv4_flags", "struct ipv4_flags *|ipv4_flags *", 0, 0, (void*)&_wrap_class_ipv4_flags, 0};
static swig_type_info _swigt__p_ipv4_network = {"_p_ipv4_network", "struct ipv4_network *|ipv4_network *", 0, 0, (void*)&_wrap_class_ipv4_network, 0};
static swig_type_info _swigt__p_ipv4_payload = {"_p_ipv4_payload", "struct ipv4_payload *|ipv4_payload *", 0, 0, (void*)&_wrap_class_ipv4_payload, 0};
static swig_type_info _swigt__p_packet = {"_p_packet", "struct packet *", 0, 0, (void*)0, 0};

static swig_type_info *swig_type_initial[] = {
  &_swigt__p_ipv4,
  &_swigt__p_ipv4_addr,
  &_swigt__p_ipv4_flags,
  &_swigt__p_ipv4_network,
  &_swigt__p_ipv4_payload,
  &_swigt__p_packet,
};

static swig_cast_info _swigc__p_ipv4[] = {  {&_swigt__p_ipv4, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_ipv4_addr[] = {  {&_swigt__p_ipv4_addr, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_ipv4_flags[] = {  {&_swigt__p_ipv4_flags, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_ipv4_network[] = {  {&_swigt__p_ipv4_network, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_ipv4_payload[] = {  {&_swigt__p_ipv4_payload, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_packet[] = {  {&_swigt__p_packet, 0, 0, 0},{0, 0, 0, 0}};

static swig_cast_info *swig_cast_initial[] = {
  _swigc__p_ipv4,
  _swigc__p_ipv4_addr,
  _swigc__p_ipv4_flags,
  _swigc__p_ipv4_network,
  _swigc__p_ipv4_payload,
  _swigc__p_packet,
};


/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (END) -------- */

/* -----------------------------------------------------------------------------
 * Type initialization:
 * This problem is tough by the requirement that no dynamic 
 * memory is used. Also, since swig_type_info structures store pointers to 
 * swig_cast_info structures and swig_cast_info structures store pointers back
 * to swig_type_info structures, we need some lookup code at initialization. 
 * The idea is that swig generates all the structures that are needed. 
 * The runtime then collects these partially filled structures. 
 * The SWIG_InitializeModule function takes these initial arrays out of 
 * swig_module, and does all the lookup, filling in the swig_module.types
 * array with the correct data and linking the correct swig_cast_info
 * structures together.
 *
 * The generated swig_type_info structures are assigned staticly to an initial 
 * array. We just loop through that array, and handle each type individually.
 * First we lookup if this type has been already loaded, and if so, use the
 * loaded structure instead of the generated one. Then we have to fill in the
 * cast linked list. The cast data is initially stored in something like a
 * two-dimensional array. Each row corresponds to a type (there are the same
 * number of rows as there are in the swig_type_initial array). Each entry in
 * a column is one of the swig_cast_info structures for that type.
 * The cast_initial array is actually an array of arrays, because each row has
 * a variable number of columns. So to actually build the cast linked list,
 * we find the array of casts associated with the type, and loop through it 
 * adding the casts to the list. The one last trick we need to do is making
 * sure the type pointer in the swig_cast_info struct is correct.
 *
 * First off, we lookup the cast->type name to see if it is already loaded. 
 * There are three cases to handle:
 *  1) If the cast->type has already been loaded AND the type we are adding
 *     casting info to has not been loaded (it is in this module), THEN we
 *     replace the cast->type pointer with the type pointer that has already
 *     been loaded.
 *  2) If BOTH types (the one we are adding casting info to, and the 
 *     cast->type) are loaded, THEN the cast info has already been loaded by
 *     the previous module so we just ignore it.
 *  3) Finally, if cast->type has not already been loaded, then we add that
 *     swig_cast_info to the linked list (because the cast->type) pointer will
 *     be correct.
 * ----------------------------------------------------------------------------- */

#ifdef __cplusplus
extern "C" {
#if 0
} /* c-mode */
#endif
#endif

#if 0
#define SWIGRUNTIME_DEBUG
#endif


SWIGRUNTIME void
SWIG_InitializeModule(void *clientdata) {
  size_t i;
  swig_module_info *module_head, *iter;
  int found, init;

  clientdata = clientdata;

  /* check to see if the circular list has been setup, if not, set it up */
  if (swig_module.next==0) {
    /* Initialize the swig_module */
    swig_module.type_initial = swig_type_initial;
    swig_module.cast_initial = swig_cast_initial;
    swig_module.next = &swig_module;
    init = 1;
  } else {
    init = 0;
  }

  /* Try and load any already created modules */
  module_head = SWIG_GetModule(clientdata);
  if (!module_head) {
    /* This is the first module loaded for this interpreter */
    /* so set the swig module into the interpreter */
    SWIG_SetModule(clientdata, &swig_module);
    module_head = &swig_module;
  } else {
    /* the interpreter has loaded a SWIG module, but has it loaded this one? */
    found=0;
    iter=module_head;
    do {
      if (iter==&swig_module) {
        found=1;
        break;
      }
      iter=iter->next;
    } while (iter!= module_head);

    /* if the is found in the list, then all is done and we may leave */
    if (found) return;
    /* otherwise we must add out module into the list */
    swig_module.next = module_head->next;
    module_head->next = &swig_module;
  }

  /* When multiple interpeters are used, a module could have already been initialized in
     a different interpreter, but not yet have a pointer in this interpreter.
     In this case, we do not want to continue adding types... everything should be
     set up already */
  if (init == 0) return;

  /* Now work on filling in swig_module.types */
#ifdef SWIGRUNTIME_DEBUG
  printf("SWIG_InitializeModule: size %d\n", swig_module.size);
#endif
  for (i = 0; i < swig_module.size; ++i) {
    swig_type_info *type = 0;
    swig_type_info *ret;
    swig_cast_info *cast;
  
#ifdef SWIGRUNTIME_DEBUG
    printf("SWIG_InitializeModule: type %d %s\n", i, swig_module.type_initial[i]->name);
#endif

    /* if there is another module already loaded */
    if (swig_module.next != &swig_module) {
      type = SWIG_MangledTypeQueryModule(swig_module.next, &swig_module, swig_module.type_initial[i]->name);
    }
    if (type) {
      /* Overwrite clientdata field */
#ifdef SWIGRUNTIME_DEBUG
      printf("SWIG_InitializeModule: found type %s\n", type->name);
#endif
      if (swig_module.type_initial[i]->clientdata) {
	type->clientdata = swig_module.type_initial[i]->clientdata;
#ifdef SWIGRUNTIME_DEBUG
      printf("SWIG_InitializeModule: found and overwrite type %s \n", type->name);
#endif
      }
    } else {
      type = swig_module.type_initial[i];
    }

    /* Insert casting types */
    cast = swig_module.cast_initial[i];
    while (cast->type) {
    
      /* Don't need to add information already in the list */
      ret = 0;
#ifdef SWIGRUNTIME_DEBUG
      printf("SWIG_InitializeModule: look cast %s\n", cast->type->name);
#endif
      if (swig_module.next != &swig_module) {
        ret = SWIG_MangledTypeQueryModule(swig_module.next, &swig_module, cast->type->name);
#ifdef SWIGRUNTIME_DEBUG
	if (ret) printf("SWIG_InitializeModule: found cast %s\n", ret->name);
#endif
      }
      if (ret) {
	if (type == swig_module.type_initial[i]) {
#ifdef SWIGRUNTIME_DEBUG
	  printf("SWIG_InitializeModule: skip old type %s\n", ret->name);
#endif
	  cast->type = ret;
	  ret = 0;
	} else {
	  /* Check for casting already in the list */
	  swig_cast_info *ocast = SWIG_TypeCheck(ret->name, type);
#ifdef SWIGRUNTIME_DEBUG
	  if (ocast) printf("SWIG_InitializeModule: skip old cast %s\n", ret->name);
#endif
	  if (!ocast) ret = 0;
	}
      }

      if (!ret) {
#ifdef SWIGRUNTIME_DEBUG
	printf("SWIG_InitializeModule: adding cast %s\n", cast->type->name);
#endif
        if (type->cast) {
          type->cast->prev = cast;
          cast->next = type->cast;
        }
        type->cast = cast;
      }
      cast++;
    }
    /* Set entry in modules->types array equal to the type */
    swig_module.types[i] = type;
  }
  swig_module.types[i] = 0;

#ifdef SWIGRUNTIME_DEBUG
  printf("**** SWIG_InitializeModule: Cast List ******\n");
  for (i = 0; i < swig_module.size; ++i) {
    int j = 0;
    swig_cast_info *cast = swig_module.cast_initial[i];
    printf("SWIG_InitializeModule: type %d %s\n", i, swig_module.type_initial[i]->name);
    while (cast->type) {
      printf("SWIG_InitializeModule: cast type %s\n", cast->type->name);
      cast++;
      ++j;
    }
  printf("---- Total casts: %d\n",j);
  }
  printf("**** SWIG_InitializeModule: Cast List ******\n");
#endif
}

/* This function will propagate the clientdata field of type to
* any new swig_type_info structures that have been added into the list
* of equivalent types.  It is like calling
* SWIG_TypeClientData(type, clientdata) a second time.
*/
SWIGRUNTIME void
SWIG_PropagateClientData(void) {
  size_t i;
  swig_cast_info *equiv;
  static int init_run = 0;

  if (init_run) return;
  init_run = 1;

  for (i = 0; i < swig_module.size; i++) {
    if (swig_module.types[i]->clientdata) {
      equiv = swig_module.types[i]->cast;
      while (equiv) {
        if (!equiv->converter) {
          if (equiv->type && !equiv->type->clientdata)
            SWIG_TypeClientData(equiv->type, swig_module.types[i]->clientdata);
        }
        equiv = equiv->next;
      }
    }
  }
}

#ifdef __cplusplus
#if 0
{ /* c-mode */
#endif
}
#endif



/* Forward declaration of where the user's %init{} gets inserted */
void SWIG_init_user(lua_State* L );
    
#ifdef __cplusplus
extern "C" {
#endif
/* this is the initialization function
  added at the very end of the code
  the function is always called SWIG_init, but an earlier #define will rename it
*/
#if ((SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUA) || (SWIG_LUA_TARGET == SWIG_LUA_FLAVOR_ELUAC))
LUALIB_API int SWIG_init(lua_State* L)
#else
SWIGEXPORT int SWIG_init(lua_State* L) /* default Lua action */
#endif
{
#if (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC) /* valid for both Lua and eLua */
  int i;
  /* start with global table */
  lua_pushglobaltable (L);
  /* SWIG's internal initalisation */
  SWIG_InitializeModule((void*)L);
  SWIG_PropagateClientData();
#endif

#if ((SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUA) && (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC))
  /* add a global fn */
  SWIG_Lua_add_function(L,"swig_type",SWIG_Lua_type);
  SWIG_Lua_add_function(L,"swig_equals",SWIG_Lua_equal);
  /* begin the module (its a table with the same name as the module) */
  SWIG_Lua_module_begin(L,SWIG_name);
  /* add commands/functions */
  for (i = 0; swig_commands[i].name; i++){
    SWIG_Lua_module_add_function(L,swig_commands[i].name,swig_commands[i].func);
  }
  /* add variables */
  for (i = 0; swig_variables[i].name; i++){
    SWIG_Lua_module_add_variable(L,swig_variables[i].name,swig_variables[i].get,swig_variables[i].set);
  }
#endif

#if (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC)
  /* set up base class pointers (the hierarchy) */
  for (i = 0; swig_types[i]; i++){
    if (swig_types[i]->clientdata){
      SWIG_Lua_init_base_class(L,(swig_lua_class*)(swig_types[i]->clientdata));
    }
  }
  /* additional registration structs & classes in lua */
  for (i = 0; swig_types[i]; i++){
    if (swig_types[i]->clientdata){
      SWIG_Lua_class_register(L,(swig_lua_class*)(swig_types[i]->clientdata));
    }
  }
#endif

#if ((SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUA) && (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC))
  /* constants */
  SWIG_Lua_InstallConstants(L,swig_constants);
#endif

#if (SWIG_LUA_TARGET != SWIG_LUA_FLAVOR_ELUAC)
  /* invoke user-specific initialization */
  SWIG_init_user(L);
  /* end module */
  /* Note: We do not clean up the stack here (Lua will do this for us). At this
     point, we have the globals table and out module table on the stack. Returning
     one value makes the module table the result of the require command. */
  return 1;
#else
  return 0;
#endif
}

#ifdef __cplusplus
}
#endif


const char* SWIG_LUACODE=
  "\n"
  "	haka.dissector {\n"
  "		name = \"ipv4\",\n"
  "		dissect = ipv4.dissect\n"
  "	}";

void SWIG_init_user(lua_State* L)
{
  /* exec Lua code if applicable */
  SWIG_Lua_dostring(L,SWIG_LUACODE);
}

